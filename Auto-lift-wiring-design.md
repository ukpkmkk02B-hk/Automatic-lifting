# 鱼缸框篮自动升降控制装置接线设计

日期：2026-05-04
状态：用户确认的暂定硬件接线方案

## 目标

本项目使用 STM32F103C8T6 控制鱼缸内框篮上下移动，使框篮内多鳍鱼实际可活动水深保持设定值，或按照设定曲线随时间缓慢变浅。控制对象是框篮相对鱼缸水面的高度，不是整体鱼缸水位。

框篮最大行程按当前机械补充规格为 100mm。两个贯穿式步进电机同步驱动框篮两侧，丝杆导程为 2mm，丝杆长度为 250mm。水深控制以目标曲线为主，闭环停止死区为 ±1mm，普通低频修正启动死区为 ±2mm，硬故障误差阈值为 ±5mm。

## 依据资料

本设计依据以下本地资料和现有工程状态：

- `Materials/UM244 使用手册V1.1.pdf`
- `Materials/42HSC1409-250NE2.pdf`
- `Materials/WF5805F 2Bar Datasheet V1.0.pdf`
- `Materials/STM32F103C8T6核心板原理图.pdf`
- `Materials/STM32F103x8B_DS_CH_V10.pdf`
- `Materials/STM32F10xxx参考手册（英文）.pdf`
- `Materials/STM32F103xx固件函数库用户手册.pdf`
- `Materials/ST-LINK+V2使用说明.pdf`
- `Materials/限位器接线.png`
- `Materials/npn型光耦隔离器-用于限位器信号输入.jpg`
- `Materials/npn型光耦隔离器-用于限位器信号输入（详细版）.jpg`
- `Materials/光耦隔离器原理图.jpg`
- `Materials/npn型光耦隔离器-用于给步进电机驱动器的拉低信号转换.jpg`
- `Materials/0.96寸4针B版本结构图.pdf`
- `Materials/0.96寸OLED规格书.pdf`
- `Materials/4-1 OLED显示屏.jpg`
- `Materials/中景园电子0.96OLED显示屏IIC接口原理图.pdf.pdf`
- `Materials/中景园电子0.96OLED显示屏_驱动芯片手册.pdf`
- `Materials/3-有源蜂鸣器/有源蜂鸣器模块原理图.png`
- `Materials/3-有源蜂鸣器/有源蜂鸣器模块实物图.png`
- `Materials/STM32F103C8T6引脚定义.xlsx`
- `Reference/WF5805_2BAR官方驱动包`
- `Reference/步进电机驱动示例`
- 现有工程中 OLED 使用 `PB8/PB9` 软件 I2C
- 现有工程中按键使用 `PB1/PB11`
- 本方案中空气参考传感器 I2C-A 使用 `PA1/PA2`，LED 改用 `PA6/PA7`

## 系统结构

系统由以下模块组成：

- STM32F103C8T6 最小系统板
- 0.96 寸 OLED 显示屏
- 三颗 WF5805F 压力传感器小板
- UM244 二相步进电机驱动器
- 两个 42HSC1409-250NE2 贯穿式丝杆步进电机
- 四个 24V NPN 限位开关
- 低电平触发有源蜂鸣器模块
- 24V 5A 开关电源
- 24V 转 5V 降压模块，用于最小系统板 `5V` 输入和 UM244 输入信号公共正端
- 最小系统板 3.3V 输出或独立 3.3V 稳压，用于 OLED、WF5805F 和蜂鸣器模块
- 驱动器输入信号电平转换电路
- 24V NPN 限位信号到 STM32 的输入型光耦隔离电路

## 压力传感器方案

WF5805F 是绝对压力传感器，读数包含大气压。为达到 ±1mm 水深控制目标，系统使用三颗 WF5805F：

| 传感器     | 位置                 | 作用                           |
| ---------- | -------------------- | ------------------------------ |
| `P_air`    | 控制盒内，接触空气   | 大气压参考                     |
| `P_tank`   | 鱼缸底部固定位置     | 计算整体鱼缸水深并提供安全判断 |
| `P_basket` | 框篮底部，随框篮移动 | 计算框篮内实际可活动水深       |

水深计算使用压力差，并扣除维护模式三传感器同空气校准得到的固定零点偏移：

```text
basket_depth_mm = ((P_basket - P_air) - zero_offset_basket) * 10.197
tank_depth_mm   = ((P_tank - P_air) - zero_offset_tank) * 10.197
```

其中压力单位为 hPa，`1 hPa` 约等于 `10.197 mmH2O`。`zero_offset_basket` 和 `zero_offset_tank` 分别来自 `P_basket-P_air`、`P_tank-P_air` 在同一空气环境中的滤波差值。

## 参数范围与默认值

以下参数作为后续代码实现的默认值和安全边界。需要让后续固件把这些值集中放在配置头文件中，便于调试后修改。

水深安全范围：

| 参数                       |  数值 | 行为                                                                                                                           |
| -------------------------- | ----: | ------------------------------------------------------------------------------------------------------------------------------ |
| `tank_min_depth_mm`        | 250mm | 鱼缸整体水深低于该值需约 10s 稳定确认；低水位判定会去除单个 TNK 极值、忽略明显跳变和 I2C-C 刚恢复样本，恢复按约 260mm 连续确认 |
| `tank_max_depth_mm`        | 450mm | 历史/诊断参考；高水位由机械溢流/漏水限位保证，固件不再因高于该值锁存 `E_TANK_HIGH`                                             |
| `basket_min_safe_depth_mm` |   5mm | 框篮内幼鱼最低安全可活动水深                                                                                                   |
| `basket_max_safe_depth_mm` | 120mm | 框篮内最高合理可活动水深                                                                                                       |
| `target_min_depth_mm`      |   5mm | 自动目标水深允许设置的最小值                                                                                                   |
| `target_max_depth_mm`      | 100mm | 自动目标水深允许设置的最大值                                                                                                   |

适应曲线参数：

| 参数               |    数值 | 说明                                                           |
| ------------------ | ------: | -------------------------------------------------------------- |
| 初始目标水深       |   100mm | 默认值；实际运行时可用上电实测水深作为起点                     |
| 最终目标水深       |    10mm | 默认目标终点                                                   |
| 默认每日变浅量     | 1mm/day | 可通过配置宏或菜单参数修改                                     |
| 最大允许每日变浅量 | 3mm/day | 防止误设置过快                                                 |
| 允许误差           |    ±1mm | 低频闭环停止死区；1-2mm 只观察并阻止每日打盹，2mm 以上小步修正 |
| 断电恢复           |  不补偿 | 重启后不追赶断电期间错过的下降量                               |

OLED 参数页允许用户调整的范围如下，保存时还会经过 Flash 参数校验：

| 参数           |    默认值 | 可调范围        | 额外约束                             |
| -------------- | --------: | --------------- | ------------------------------------ |
| 初始目标水深   |     100mm | `5.0-100.0mm`   | 不得小于最终目标水深                 |
| 最终目标水深   |      10mm | `5.0-100.0mm`   | 不得大于初始目标水深                 |
| 每日变浅量     | 1.0mm/day | `0.0-3.0mm/day` | 需与单次打盹脉冲组合满足最小打盹间隔 |
| 单次打盹脉冲数 |   8 pulse | `1-24 pulse`    | 理论打盹间隔不得小于 `5min`          |

低频闭环 `TRK`、快速掉水跟随 `DROP`、传感器失败次数、限位滤波等参数集中在 `Hardware/board_config.h`，属于编译期可配置参数；若需要调整，应修改宏定义、重新编译并重新烧录。

打盹运动参数：

| 参数           |       数值 | 说明                                  |
| -------------- | ---------: | ------------------------------------- |
| 默认单次脉冲数 |    8 pulse | 对应 0.01mm                           |
| 最大单次脉冲数 |   24 pulse | 防止一次移动过大                      |
| 默认打盹间隔   | 约 14.4min | 1mm/day、8 pulse/次时的理论间隔       |
| 最小打盹间隔   |       5min | 防止菜单误设置过频繁                  |
| 自动脉冲频率   |      800Hz | 可降级为 400Hz；8 pulse 微冲不加减速  |
| 手动移动速度   |      1mm/s | 对应 800 pulse/s，仅用于手动/维护点动 |

报警与恢复阈值：

| 参数               |       数值 | 行为                                                                                 |
| ------------------ | ---------: | ------------------------------------------------------------------------------------ |
| 水位突变阈值       |   10mm/min | 非可跟随异常突变仍报警；命令运动期间仅抑制框篮自身变化误报；普通掉水由 DROP 仲裁处理 |
| 快速掉水进入阈值   |    5mm/min | 趋势有效跨度至少 `20s` 后，鱼缸水位下降达到该速度且框篮变浅时进入 DROP               |
| 危险掉水阈值       |   30mm/min | 趋势有效跨度至少 `20s` 后，超过该速度不跟随，直接置 `E_WATER_JUMP`                   |
| 传感器连续失败次数 |       5 次 | 连续 I2C 读数失败后报警                                                              |
| I2C 恢复尝试次数   |       5 次 | 重初始化 5 次仍失败则报警                                                            |
| 重启水深差异阈值   |      2-3mm | 上电实测水深与断电前记录差异超过该范围时暂停报警                                     |
| 低水位报警静音     |       允许 | 蜂鸣器可静音，但故障状态不清除                                                       |
| 报警后手动移动     | 仅维护模式 | 报警后只能进入维护模式进行人工移动                                                   |

卡滞/过载软件保护参数：

| 参数             |   数值 | 说明                                  |
| ---------------- | -----: | ------------------------------------- |
| 最小检测累计位移 |    1mm | 累计移动达到 1mm 后检查水深趋势       |
| 期望最小水深变化 |    1mm | 累计移动后至少应看到 1mm 方向一致变化 |
| 连续卡滞次数     |   8 次 | 连续 8 次未达到变化则报警             |
| 24V 电流检测模块 | 不增加 | 当前版本不做独立电流检测              |

开机自检参数：

| 参数               | 数值               | 说明                                           |
| ------------------ | ------------------ | ---------------------------------------------- |
| 自检通过后行为     | 按断电恢复策略处理 | 满足安全条件时可自动恢复，否则暂停等待人工确认 |
| 蜂鸣器自检         | 短鸣一次           | 用于确认报警外设可用                           |
| 传感器稳定等待时间 | 10 秒              | 上电后等待压力读数稳定                         |
| Flash 参数无效     | 使用默认值         | 同时在 OLED 显示参数恢复提示                   |

## OLED 独立 I2C 与 WF5805F 三组 I2C 接线

实际采购的 WF5805F 传感器模块为 4 引脚版本，仅引出 `VDD/GND/SCL/SDA`，没有引出 `SDO/ADDR`，因此不能通过外部接线修改 I2C 地址。`Reference/WF5805_2BAR官方驱动包/wfsensor.c` 中定义 `WFSensorIICDevice 0XDA`，该值按官方示例作为 8-bit 写地址使用，对应固件内部 7-bit 地址 `0x6D`，读地址为 `0xDB`。

由于三颗 WF5805F 地址相同，任意两颗 WF5805F 不能挂在同一条 I2C 总线上，否则会产生地址冲突。当前版本不使用 I2C 多路复用器，而是使用三组压力传感器软件 I2C，把三颗 WF5805F 分别隔离到不同总线。OLED 不再与任何 WF5805F 共用 I2C；OLED 仍使用原有 `PB8/PB9` 软件 I2C。根据 `Materials/最小系统板.png` 的引脚位置，压力传感器 I2C 尽量使用相邻引脚，降低接线复杂度。

| I2C 总线 | STM32 引脚           | 设备       | 地址来源           | I2C 地址                                                             |
| -------- | -------------------- | ---------- | ------------------ | -------------------------------------------------------------------- |
| OLED-I2C | `PB8=SCL`, `PB9=SDA` | OLED       | 固定               | 7-bit `0x3C`，代码由 `BOARD_OLED_ADDR_7BIT` 派生 8-bit 写地址 `0x78` |
| I2C-A    | `PA1=SCL`, `PA2=SDA` | `P_air`    | 4 引脚模块固定地址 | 7-bit `0x6D`，8-bit 写地址 `0xDA`                                    |
| I2C-B    | `PB6=SCL`, `PB7=SDA` | `P_basket` | 4 引脚模块固定地址 | 7-bit `0x6D`，8-bit 写地址 `0xDA`                                    |
| I2C-C    | `PA8=SCL`, `PA9=SDA` | `P_tank`   | 4 引脚模块固定地址 | 7-bit `0x6D`，8-bit 写地址 `0xDA`                                    |

每颗 WF5805F 模块接线：

| WF5805F 模块引脚 | 接法              |
| ---------------- | ----------------- |
| `VDD`            | 3.3V              |
| `GND`            | GND               |
| `SCL`            | 对应 I2C 总线 SCL |
| `SDA`            | 对应 I2C 总线 SDA |

上拉电阻：

| 总线      | 上拉                                      |
| --------- | ----------------------------------------- |
| `PB8/PB9` | OLED 的`SCL/SDA` 各 4.7k 上拉到 3.3V      |
| `PA1/PA2` | `P_air` 的 `SCL/SDA` 各 4.7k 上拉到 3.3V  |
| `PB6/PB7` | `P_basket`的`SCL/SDA` 各 4.7k 上拉到 3.3V |
| `PA8/PA9` | `P_tank`的`SCL/SDA` 各 4.7k 上拉到 3.3V   |

如果采购模块或 OLED 模块自身已经带 I2C 上拉电阻，需要实测或查原理图确认总线上拉阻值。每条总线只建议保留一组 4.7k 左右的上拉；若多个模块都带上拉，应避免总上拉过强。

## WF5805F 通信与数据处理摘要

WF5805F 驱动必须按 `Materials/WF5805F 2Bar Datasheet V1.0.pdf` 和 `Reference/WF5805_2BAR官方驱动包` 实现 I2C 读数，不允许把未验证的寄存器或命令写死为猜测值。后续代码生成前，Codex 需要先阅读 PDF 和官方驱动包，并在实现说明中摘录所用命令、返回字节含义和状态位判断。

当前参考驱动中，`WFSensor_indicateGroupConvert()` 写寄存器 `0x30`、数据 `0x0A` 启动转换，`WFSensor_WaitFinish()` 读寄存器 `0x02` 判断完成状态，`WFSensor_getTPData()` 从寄存器 `0x06` 连续读取 5 字节。该流程可作为实现依据，但新固件必须增加 I2C 超时、错误返回和总线恢复，不允许照搬参考驱动中的阻塞等待。

固件侧统一数据接口：

| 项目     | 设计                                                                                      |
| -------- | ----------------------------------------------------------------------------------------- |
| I2C 速率 | 软件 I2C，默认 100kHz 起步                                                                |
| 地址格式 | 代码内部统一使用 7-bit 地址；WF5805F 固定为 `0x6D`，官方驱动中的 `0XDA` 是 8-bit 写地址   |
| 压力单位 | 驱动输出 `pressure_hpa_x100`，即 hPa 放大 100 倍的整数                                    |
| 水深单位 | 水深模块输出 `depth_mm_x10`，即 mm 放大 10 倍的整数                                       |
| 轮询周期 | 正常自动运行每 1 秒读取一次；每次打盹移动前后必须各读取一次                               |
| 滤波     | 每个传感器保留最近 5 次有效读数，使用滑动平均值参与水深计算；打盹前后读数仍进入同一滤波器 |
| 失败计数 | 每颗传感器独立记录连续失败次数；同一总线独立记录恢复失败次数                              |

压力差到水深的整数换算：

```text
depth_mm_x10 = pressure_diff_hpa_x100 * 10197 / 10000
```

其中 `pressure_diff_hpa_x100 = pressure_sensor_hpa_x100 - pressure_air_hpa_x100`。若压力差对应水深小于 `-2mm`，按传感器或安装异常处理。

## WF5805F PCB 与防水封装要求

当前使用 4 引脚 WF5805F 模块，不再依赖裸芯片的 `CSB`、`SDO/ADDR`、`INT` 等引脚。模块需要做好防水封装并引出 `VDD/GND/SCL/SDA` 四根线。

每块 WF5805F 模块封装必须满足：

- `VDD-GND` 旁路 100nF 电容，靠近芯片放置
- 四线接口：3.3V、GND、SCL、SDA
- 防水线缆应力释放
- 电路区域完全防水密封
- 压力敏感区域必须能接收到外部水压

封装必须避免把压力敏感结构完全刚性灌死。可采用柔性隔离膜、油腔或软胶膜等结构传递水压，同时防止水进入电路。空气参考传感器不下水，安装在控制盒内，保持与外界空气连通，避免盒内完全密封导致参考压力滞后。

## 校准策略

当前版本采用轻量校准策略，不要求用户手动输入鱼缸实际水深或框篮实际水深。系统依赖三颗压力传感器的差压读数计算当前水深，并在开机和维护模式中显示实测结果。

校准功能：

| 校准项       | 方式                                                     | 结果                                                                   |
| ------------ | -------------------------------------------------------- | ---------------------------------------------------------------------- |
| 空气零点校准 | 三颗传感器同时暴露在同一空气环境中执行维护菜单 `CAL AIR` | 记录 `BAS/TNK` 相对 `AIR` 的零点偏移，即 `zero_offset_basket/tank`     |
| 鱼缸水深校准 | 不手动输入实际水深                                       | 使用 `((P_tank - P_air) - zero_offset_tank) * 10.197` 计算整体水深     |
| 框篮水深校准 | 不手动输入实际水深                                       | 使用 `((P_basket - P_air) - zero_offset_basket) * 10.197` 计算框篮水深 |

维护模式允许反复执行校准并覆盖 Flash 中保存的校准参数。`CAL AIR` 只在三路滤波压力都有效且新鲜时保存；如果任一路传感器 I2C 读数失败、压力值明显超出物理范围或状态无效，拒绝保存并按传感器/参数故障处理。

初始水深处理：

- 系统不要求用户预先写死初始水深。
- 开机自检通过并等待传感器稳定后，系统显示当前 `tank_depth_mm` 和 `basket_depth_mm`。
- 用户确认后，系统可把当前实测 `basket_depth_mm` 作为实际初始水深。
- 自动目标曲线仍受 `target_min_depth_mm`、`target_max_depth_mm`、最终目标水深和每日变浅量限制。

## 步进电机与 UM244 驱动器

电机资料参数：

| 项目     | 数值               |
| -------- | ------------------ |
| 型号     | `42HSC1409-250NE2` |
| 步距角   | 1.8 度             |
| 额定电流 | 1.33A/相           |
| 额定电压 | 2.93V              |
| 相电阻   | 2.2Ω               |
| 相电感   | 3.5mH              |
| 保持转矩 | ≥270mN.m           |
| 丝杆导程 | 2mm                |
| 标称步距 | 0.01mm/step        |

UM244 供电：

| UM244 端子    | 接线                       |
| ------------- | -------------------------- |
| `V+`          | 24V 电源正                 |
| `V-`          | 24V 电源负                 |
| `A+ A- B+ B-` | 接两个电机并联后的两相绕组 |

两个电机并联到同一个 UM244 驱动器输出。按 `42HSC1409-250NE2.pdf` 电气原理图，`A-C` 为一组绕组，`B-D` 为一组绕组；对应线色为红-黄、绿-蓝。先按以下方式接线，并在低速低电流下确认方向：

| 图纸标识 / 电机线色 | 接线建议                   |
| ------------------- | -------------------------- |
| `A` / 红            | 接 UM244 `A+`              |
| `C` / 黄            | 接 UM244 `A-`              |
| `B` / 绿            | 接 UM244 `B+`              |
| `D` / 蓝            | 接 UM244 `B-`              |
| 两个电机            | 同色线并到同一个驱动器端子 |

若两个电机方向不一致，交换其中一个电机同一绕组内的两根线，例如只交换该电机的红/黄，或只交换绿/蓝。不要把红/绿或黄/蓝当作一相。

UM244 拨码建议：

| 项目          | 设置                                                     |
| ------------- | -------------------------------------------------------- |
| 运行电流      | 先设 2.5A：`SW1=ON`, `SW2=ON`, `SW3=OFF`                 |
| 细分/速度模式 | `SW4=OFF`，细分设置模式                                  |
| 细分          | 1600 pulse/rev：`SW5=OFF`, `SW6=OFF`, `SW7=ON`, `SW8=ON` |
| 位移换算      | 1600 pulse/rev ÷ 2mm = 800 pulse/mm                      |

若 2.5A 扭矩不足，可在确认温升和机械阻力正常后试 3.0A。不要按电源 5A 能力直接把驱动器电流拨到最大。

电机资料中的 `0.01mm/step` 对应 200 full-step/rev 时的整步位移。当前推荐 UM244 使用 1600 pulse/rev 细分，因此 1 个输入脉冲对应：

```text
1 pulse = 2mm / 1600 = 0.00125mm
0.01mm  = 8 pulse
1mm     = 800 pulse
```

## 自动打盹运动策略

自动模式不使用连续极低速运行。多鳍鱼幼鱼身体直径不到 10mm，连续移动或一次移动过大会造成不必要扰动。系统采用“打盹模式”：

1. 大部分时间电机处于静止保持状态。
2. 每隔固定时间唤醒一次运动控制。
3. 读取三颗压力传感器并检查四个限位器状态。
4. 若状态正常且当天仍有下降计划额度，以舒适、安全、低抖动的脉冲频率执行极小位移。
5. 位移完成后立即停止发送脉冲，保存累计进度，回到静止保持状态。

默认目标曲线暂定为框篮内实际水深每天变浅 `1mm/day`。按 1600 pulse/rev 细分计算，每天需要累计上升 `800 pulse`。

推荐自动打盹参数：

| 参数           |           默认值 | 说明                                                          |
| -------------- | ---------------: | ------------------------------------------------------------- |
| 每日变浅量     |        `1mm/day` | 可后续通过菜单设置                                            |
| 最大每日变浅量 |        `3mm/day` | 参数设置不得超过该值                                          |
| 自动细分       | `1600 pulse/rev` | 保持 UM244 细分，运动更平滑                                   |
| 每日总脉冲     |  `800 pulse/day` | 对应 1mm                                                      |
| 精细打盹单位   |        `1 pulse` | 单次位移 0.00125mm，约每 108 秒执行一次                       |
| 稳妥打盹单位   |        `8 pulse` | 单次位移 0.01mm，约每 14.4 分钟执行一次                       |
| 默认原型模式   |     `8 pulse/次` | 先验证机械是否真实响应；若运动过于可见，再切到 1 pulse/次     |
| 最大单次打盹   |    `24 pulse/次` | 防止一次移动过大                                              |
| 最小打盹间隔   |           `5min` | 防止设置过频繁                                                |
| 自动脉冲频率   |     默认 `800Hz` | 可降级为 `400Hz`；8 pulse 约 10ms，24 pulse 约 30ms，不加减速 |

自动模式优先采用计划位移累加，而不是根据传感器噪声频繁追调。压力传感器用于确认实际水深、校正长期误差和检测异常。若传感器显示实际水深偏离目标超过允许范围，系统应暂停自动下降并报警，等待人工确认，避免因传感器漂移导致过度移动。

当前自动控制已升级为受限闭环仲裁。普通每日打盹仍是目标曲线的主路径；自动状态内新增两个受限模式：

| 模式         | OLED         | 触发条件                                                       | 运动方向   | 结束行为                                                   |
| ------------ | ------------ | -------------------------------------------------------------- | ---------- | ---------------------------------------------------------- |
| 每日打盹     | `AUTO`/`NAP` | 到达打盹时间且当天仍有变浅额度                                 | 只上升     | 回到自动等待                                               |
| 低频闭环修正 | `TRK`        | 每 3min 检查一次，误差达到 ±2.0mm；1-2mm 观察并阻止每日打盹    | 上升或下降 | 修正到 1.0mm 内回 `AUTO`                                   |
| 快速掉水跟随 | `DROP`       | 有效趋势至少 20s，鱼缸水位 5-30mm/min 下降且框篮水深变浅 2.0mm | 只下降     | 水位稳定、超时或超距离后进入 `PAUSED` 并显示 `CHECK WATER` |

自动控制优先级固定为：限位、传感器/I2C、水位越界、电机释放和位置不可信等硬安全故障优先；其次是快速掉水跟随；再次是低频闭环修正；最后才是每日打盹或等待显示。任何模式都不能直接翻转 STEP GPIO，必须通过 UM244 有限脉冲接口执行。

自动仲裁流程应按以下顺序理解：

```text
安全联锁
限位 / 传感器 / I2C / 水位范围 / 位置可信 / 电机释放
        │
        ▼
自动控制仲裁器
        │
        ├─ 趋势确认后的危险掉水或不可跟随突变：进入 FAULT
        ├─ 快速掉水跟随 DROP：框篮下降补深，结束后 PAUSED + CHECK WATER
        ├─ 低频闭环修正 TRK：小步上升或下降，回到 ±1.0mm 内停止
        ├─ 每日打盹 NAP：按目标曲线执行计划性上升变浅
        └─ 无动作：AUTO 等待显示
```

`auto_control` 每轮只允许输出一个仲裁结果，结果类型限定为 `MOVE`、`PAUSE`、`FAULT` 或 `NONE`；实际 STEP 输出仍由 `app_state` 统一调用 `stepper_um244` 有限脉冲接口完成，避免 `DROP`、`TRK`、`NAP` 同时争用电机。今日进度 `today_pulses_done` 只统计自动变浅动作：每日打盹向上和 `TRK` 向上修正计入，并共享当日剩余变浅额度；`TRK` 向下补深、`DROP` 跟随下降、手动点动和回零都不计入。

低频闭环参数落在 `Hardware/board_config.h` 中：检查周期 `180000ms`、启动死区 `20mm_x10`、停止死区 `10mm_x10`、单步 `160/200/400 pulse`、速度 `800Hz`、稳定等待 `15000ms`、稳定采样 `3` 次、稳定变化量 `5mm_x10`、验证超时 `60000ms`、连续失败 `5` 次、硬误差 `50mm_x10`、每小时/每日额外修正上限 `2400/8000 pulse`。

快速掉水跟随参数也集中在 `Hardware/board_config.h` 中：趋势窗口 `30000ms`、趋势缓存 `40` 个 1s 样本、最小样本 `3`、DROP 入口/危险掉水报警最小趋势跨度 `20000ms`、进入/危险/稳定速度 `5/30/2 mm/min`、启动/停止误差 `20/10mm_x10`、单步 `400-800 pulse`、速度 `800Hz`、连续/硬最大时间 `30000/60000ms`、单事件最大距离 `24000 pulse`、稳定等待 `15000ms`。`5-30mm/min` 鱼缸掉水只有在框篮同步变浅时才允许 DROP 向下跟随；鱼缸掉水但框篮未按阈值变浅时直接置 `E_WATER_JUMP`。

这里的“休眠”指运动控制休眠：停止 STEP 脉冲、保持当前位置、等待下一次唤醒。默认不要通过 `MF` 释放电机电流，因为框篮是垂直负载，释放电机可能导致位置变化。只有在确认丝杆自锁可靠、框篮不会滑动，或增加机械制动后，才允许在维护模式中释放电机。

## 输入消抖与运动防抖参数

自动打盹从“慢速滴答”改为“快速微冲”。由于单次自动运动默认只有 `8 pulse`，最大也只有 `24 pulse`，自动打盹不做加减速曲线，改用稳定定时脉冲快速完成微小位移。

运动防抖规则：

| 项目                | 参数                                  |
| ------------------- | ------------------------------------- |
| 自动 STEP 频率      | 默认 `800Hz`，可降级为 `400Hz`        |
| STEP 输出方式       | 定时器或严格定时输出，推荐 50% 占空比 |
| `8 pulse` 微冲时长  | 约 `10ms` @ 800Hz；约 `20ms` @ 400Hz  |
| `24 pulse` 微冲时长 | 约 `30ms` @ 800Hz；约 `60ms` @ 400Hz  |
| 自动微冲加减速      | 不启用；仅限 `1-24 pulse` 自动打盹    |
| `DIR` 建立时间      | 改变方向后至少等待 `5ms` 再发 STEP    |
| `DIR` 保持时间      | 最后一个 STEP 后至少保持 `5ms`        |
| `NAP_MOVE` 忙锁     | 执行中禁止再次触发新的打盹动作        |
| `MF` 状态           | 自动打盹前后均保持不释放电机          |

按键消抖参数：

| 项目                  | 参数                      |
| --------------------- | ------------------------- |
| 按键扫描周期          | `10ms`                    |
| 稳定确认时间          | `25ms`                    |
| 短按有效时间          | `25-1000ms`               |
| 普通长按              | `1000ms` 以上             |
| 维护模式入口长按      | `PB10` 按下 `3000ms` 以上 |
| 参数连加/连减重复间隔 | `200ms`                   |

限位输入滤波参数：

| 项目         | 参数                                                                   |
| ------------ | ---------------------------------------------------------------------- |
| 限位采样周期 | `5-10ms`                                                               |
| 触发确认     | 连续稳定触发 `20ms`                                                    |
| 释放确认     | 连续稳定释放 `50ms`                                                    |
| 运动中保护   | 原始采样检测到当前方向对应限位有效时立即停止，再用滤波结果确认故障显示 |

## UM244 控制信号接线

UM244 的 `PU/DR/MF` 输入高电平要求大于 4V，STM32 3.3V GPIO 不应直接驱动。推荐使用 5V 共阳接法，并用单路 NPN 光耦隔离模块下拉输入负端。这里的 +5V 来自同一个 24V 转 5V 降压模块，该 5V 同时给 STM32 最小系统板 `5V` 引脚供电。

`Materials/npn型光耦隔离器-用于给步进电机驱动器的拉低信号转换.jpg` 所示模块为“单片机或控制器隔离控制其它设备”的 NPN 低电平输出模块。UM244 手册标明 `PU-/DR-` 输入电阻为 `220Ω`，5V 共阳下拉时电流约 `20-23mA`。结合 `Materials/npn型光耦隔离器-用于限位器信号输入（详细版）.jpg` 中同类模块参数，输出端最大电流可到 `50mA`，可以承载 UM244 `PU-/DR-/MF-` 的低电平信号转换。

由于 UM244 要求低电平为 `0-0.5V`，调试时必须在接上 UM244 后实测 `PU-/DR-/MF-` 的低电平电压。若任一路低电平高于 `0.5V`，或模块明显发热，应停止使用该模块直接驱动，改用三极管/MOSFET 后级。

每个 UM244 控制信号使用一块独立单路模块，至少需要三块模块：`STEP -> PU-`、`DIR -> DR-`、`MOTOR_RELEASE -> MF-`。若只控制步进和方向、不使用电机释放，也仍建议保留 `MF` 模块并由软件保持不释放状态，便于维护模式统一控制。

同类模块最大应用信号频率标称约 `5kHz`，当前 `800Hz` 自动微冲、`400Hz` 回零和 `1mm/s` 手动速度对应的脉冲频率均低于该限制。若后续把 STEP 频率提高到接近或超过 `5kHz`，应改用高速光耦或三极管/MOSFET 直驱电平转换。

STM32 侧接法：

| 单路光耦输入侧 | 接法                                     |
| -------------- | ---------------------------------------- |
| `VCC(3.3/5V)`  | STM32 `3.3V`，不要接 5V                  |
| `IO`           | STM32 `PA3/PA4/PA5`                      |
| 触发逻辑       | STM32 输出低电平时，光耦导通，输出侧变低 |

光耦输出侧接法：

| 单路光耦输出侧      | 接法                                       |
| ------------------- | ------------------------------------------ |
| `3V-24V` / 信号电源 | 24V 转 5V 降压模块输出 +5V                 |
| `控制信号` / `OUT`  | 分别接 UM244 `PU-`、`DR-`、`MF-`           |
| `GND(0V)` / 信号地  | 5V GND，与 STM32 GND、UM244 信号参考地共地 |

推荐接法：

| UM244 端子     | 接法                                         |
| -------------- | -------------------------------------------- |
| `PU+`          | 24V 转 5V 降压模块输出 +5V                   |
| `DR+`          | 24V 转 5V 降压模块输出 +5V                   |
| `MF+`          | 24V 转 5V 降压模块输出 +5V                   |
| `PU-`          | STEP 单路光耦输出 `OUT`，由 STM32 `PA3` 控制 |
| `DR-`          | DIR 单路光耦输出 `OUT`，由 STM32 `PA4` 控制  |
| `MF-`          | MF 单路光耦输出 `OUT`，由 STM32 `PA5` 控制   |
| 光耦输出侧 GND | 5V GND，与 STM32 GND 共地                    |
| STM32 GND      | 与 5V GND 共地                               |

该模块会反相：STM32 输出低电平时，UM244 对应负端被拉低。`PU-` 为下降沿有效，因此 STEP 空闲时 STM32 `PA3` 保持高电平，输出一个脉冲时拉低再恢复高电平。`MF` 为电机释放信号，低电平有效；STM32 `PA5` 上电初始化后必须保持高电平，默认不释放电机，仅在维护、暂停释放或故障处理时使能释放。若担心复位期间误释放，可在 `PA5` 到 3.3V 增加 `10k` 上拉。

## 限位开关接线

四个限位开关均为资料图中的 24V NPN 输出，触发后黑线输出低电平。限位输入隔离模块必须选用“24V NPN 输入型光耦隔离模块”：24V 传感器侧驱动光耦输入，STM32 侧输出为低有效信号。

`Materials/npn型光耦隔离器-用于限位器信号输入.jpg` 所示模块标注为“外部信号隔离输入单片机”和“NPN 信号输入示意图”，逻辑为输入侧低电平时光耦导通，输出侧 `OUT-IO` 输出低电平。`Materials/光耦隔离器原理图.jpg` 显示输出侧为光耦三极管加 `R2` 上拉，`OUT` 在光耦导通时被拉低。该模块用途与当前 24V NPN 限位器输入匹配，可以作为四个限位器到 STM32 的隔离输入模块。

`Materials/npn型光耦隔离器-用于限位器信号输入（详细版）.jpg` 标明输入信号电压为 `DC 3V-5V/12V/24V` 可选，因此本项目必须使用 24V 输入版本。若模块只是 3.3V/5V 或 12V 输入版本，不能直接接 24V 限位器。输出信号电压虽然标称适用于 `DC 1.8V-24V`，但接 STM32 时输出侧 `VCC` 必须接 3.3V，使板载 `R2` 只把 `OUT` 上拉到 3.3V。

每个限位器必须使用独立隔离通道。若购买的是单通道模块，需要四块模块；若购买的是多通道模块，需要四路彼此独立输入和四路独立 `OUT-IO` 输出，不能把多个限位器黑线并到同一个输入通道。

推荐的限位器接线：

| 限位器线色 | 接法                                          |
| ---------- | --------------------------------------------- |
| 棕色       | +24V                                          |
| 蓝色       | 24V 0V                                        |
| 黑色       | 光耦输入侧低电平信号端，即模块输入负端/信号端 |

光耦输入侧：

| 光耦输入侧      | 接法       |
| --------------- | ---------- |
| 输入正端/+      | +24V       |
| 输入负端/信号端 | 限位器黑线 |

光耦输出到 STM32：

| 光耦输出          | 接法                                                  |
| ----------------- | ----------------------------------------------------- |
| 输出侧 VCC        | STM32 3.3V                                            |
| 输出 GND          | STM32 GND                                             |
| `OUT-IO`/输出信号 | STM32 GPIO                                            |
| GPIO 上拉         | 优先使用模块板载 `R2` 上拉；需要外接时只能上拉到 3.3V |

光耦输出侧按低有效输入处理。输出侧 `VCC` 和任何上拉都必须接 3.3V，不能接 5V 或 24V。

限位逻辑：

| 状态   | STM32 读数 |
| ------ | ---------- |
| 未触发 | 高电平 `1` |
| 触发   | 低电平 `0` |

安全逻辑：

- 任意一个上限位触发，禁止继续上升
- 任意一个下限位触发，禁止继续下降
- 左右同方向限位状态不一致时立即停机报警，提示可能存在框篮倾斜、两侧不同步或限位安装偏差

## STM32 引脚分配

引脚分配优先参考 `Materials/最小系统板.png` 的物理位置：OLED 固定在底部相邻的 `PB8/PB9`；空气参考传感器占用 `PA1/PA2`；LED 改用 `PA6/PA7`；限位器集中在底部相邻的 `PB12-PB15`；UM244 控制线集中在顶部相邻的 `PA3/PA4/PA5`；四个按键集中在顶部相邻区域 `PB11/PB10/PB1/PB0`。

| 功能          | STM32 引脚       | 说明               |
| ------------- | ---------------- | ------------------ |
| OLED-I2C SCL  | `PB8`            | OLED               |
| OLED-I2C SDA  | `PB9`            | OLED               |
| I2C-A SCL     | `PA1`            | 空气参考 WF5805F   |
| I2C-A SDA     | `PA2`            | 空气参考 WF5805F   |
| I2C-B SCL     | `PB6`            | 框篮底部 WF5805F   |
| I2C-B SDA     | `PB7`            | 框篮底部 WF5805F   |
| I2C-C SCL     | `PA8`            | 鱼缸底部 WF5805F   |
| I2C-C SDA     | `PA9`            | 鱼缸底部 WF5805F   |
| UM244 STEP    | `PA3 / TIM2_CH4` | 定时器脉冲输出     |
| UM244 DIR     | `PA4`            | 方向控制           |
| UM244 MF/释放 | `PA5`            | 电机释放控制       |
| 左电机上限位  | `PB12`           | 光耦后低有效输入   |
| 左电机下限位  | `PB13`           | 光耦后低有效输入   |
| 右电机上限位  | `PB14`           | 光耦后低有效输入   |
| 右电机下限位  | `PB15`           | 光耦后低有效输入   |
| 按键 1        | `PB1`            | 沿用现有按键       |
| 按键 2        | `PB11`           | 沿用现有按键       |
| 暂停/确认键   | `PB10`           | 新增按键           |
| 速度/菜单键   | `PB0`            | 新增按键           |
| 有源蜂鸣器    | `PA0`            | 低电平触发报警输出 |
| LED1          | `PA6`            | 沿用现有 LED       |
| LED2          | `PA7`            | 沿用现有 LED       |
| SWDIO         | `PA13`           | 保留调试           |
| SWCLK         | `PA14`           | 保留调试           |

## LED 接线与有效电平

LED1 和 LED2 按低电平点亮方式接线：LED 正极/阳极经限流电阻接 3.3V，LED 负极/阴极分别接 STM32 GPIO。LED1 负极接 `PA6`，LED2 负极接 `PA7`。固件初始化后 GPIO 输出高电平，LED 默认熄灭；GPIO 输出低电平时点亮。不要把 LED 正极直接接 GPIO 并按高电平点亮方式接线，除非同步修改 LED 驱动的有效电平定义。

| LED 状态 | GPIO 电平  |
| -------- | ---------- |
| 熄灭     | 高电平 `1` |
| 点亮     | 低电平 `0` |

## 有源蜂鸣器接线

新增蜂鸣器使用 `Materials/3-有源蜂鸣器` 中的低电平触发有源蜂鸣器模块。该模块原理图中已经包含 S8550 三极管，STM32 GPIO 只驱动模块 `I/O` 信号脚，不直接承载蜂鸣器工作电流。

推荐接线：

| 蜂鸣器模块 | STM32 最小系统板 |
| ---------- | ---------------- |
| `VCC`      | 3.3V             |
| `GND`      | GND              |
| `I/O`      | `PA0`            |

控制逻辑：

| `PA0` 输出 | 蜂鸣器状态 |
| ---------- | ---------- |
| 高电平 `1` | 关闭       |
| 低电平 `0` | 鸣叫       |

`PA0` 初始化为推挽输出，默认置高电平，避免上电误鸣叫。由于该模块是 PNP 高边驱动结构，若 STM32 的 3.3V GPIO 直接接模块 `I/O`，模块 `VCC` 应接 3.3V。不要在直接驱动方案中把模块 `VCC` 接 5V，否则 STM32 的 3.3V 高电平可能无法可靠关闭蜂鸣器。若后续确实需要 5V 供电提高音量，应增加电平转换或改用独立三极管驱动。若需要在 STM32 复位期间也保持安静，可在蜂鸣器 `I/O` 到 3.3V 增加 10k 上拉电阻。

## 电源与共地

电源结构：

| 电源                                 | 用途                                                       |
| ------------------------------------ | ---------------------------------------------------------- |
| 220V AC 转 24V 5A                    | UM244 驱动器和 24V 限位器                                  |
| 24V 转 5V 降压                       | 最小系统板 `5V` 输入、UM244 `PU+/DR+/MF+` 输入信号公共正端 |
| 最小系统板 3.3V 输出或独立 3.3V 稳压 | STM32 3.3V 逻辑、OLED、WF5805F、有源蜂鸣器模块             |

24V 5A 电源可以使用。5A 是电源可提供的最大电流能力，不会主动灌入驱动器。UM244 的电流上限由拨码和负载决定，调试时从 2.5A 运行电流开始。

24V 转 5V 降压模块输出应先空载调到稳定 5V，再接入最小系统板 `5V` 引脚。不要把 5V 直接接到最小系统板 `3.3V` 引脚、WF5805F `VDD`、OLED `VCC` 或蜂鸣器 `VCC`。这些低压外设仍按 3.3V 供电。

若直接使用最小系统板的 3.3V 引脚给 OLED、三颗 WF5805F 和蜂鸣器模块供电，需要确认板载 3.3V 稳压器的电流余量和温升。若余量不足，应增加独立 3.3V 稳压模块；不要在没有隔离或防倒灌设计时，把外部 3.3V 和最小系统板板载 3.3V 输出硬并联。

驱动器功率地、5V 信号地、STM32 地需要建立明确参考关系。推荐将 24V 电源负端、24V 转 5V 降压模块 GND、最小系统板 GND 和 UM244 信号输入侧参考地连接到同一系统地。限位器 24V 输入侧通过光耦隔离，不直接把 24V 信号接入 STM32。

建议在 24V 电源输出端增加保险丝或自恢复保险丝，并给 STM32/传感器电源增加独立滤波，减少步进电机噪声影响 I2C。

建议在设备外壳上设置清晰可操作的总电源开关或 24V 急停断电开关。软件暂停和故障停机不能替代硬件断电；当出现机械卡滞、线缆进水、异常噪声或需要人工接触运动机构时，应先切断 24V 驱动电源。

## 断电与恢复策略

软件策略：

- 断电重启后的自检阶段默认保持电机不动。
- 上电后读取三颗 WF5805F、四个限位状态和 Flash 保存的运行状态。
- 从 Flash 读取目标曲线、累计上电运行时间、当前目标水深、估算机械位置、今日已执行脉冲数、上次成功打盹移动时间和最近一次有效水深。
- 当前版本不增加 RTC 模块，系统只按上电运行时间计算运行天数和每日下降进度。
- 断电期间错过的下降量不补偿，不做追赶运动，断电期间也不累计运行天数。
- 若上次断电前处于 `AUTO_RUN` 或 `NAP_WAIT`，并且开机自检通过、传感器正常、限位正常、上电实测水深与断电前最近一次有效水深差异不超过 `3mm`、位置可信度有效，则自检结束后自动恢复自动运行。
- 若 Flash 中读到 `PARAM_STORE_APP_NAP_MOVE`，包括普通有限运动中断或保守 v1 复用的 DROP 恢复标记，重启后不自动继续运动，不自动进入 `AUTO`；固件显示 `CHECK WATER`，标记位置不可信并进入暂停或故障，要求人工检查水位和位置后再恢复。
- 自动恢复时，以上电时实测 `basket_depth_mm` 作为当前实际水深，从当前时刻重新计算下一次打盹间隔。
- 若上次断电前处于 `PAUSED`，自检通过后仍进入暂停状态，等待人工确认。
- 若上次断电前处于 `MAINTENANCE`、`MOTOR_RELEASE` 或 `FAULT`，重启后不自动运行，必须人工确认并处理。
- 若传感器异常、限位异常、位置可信度不足、Flash 参数无效或水深差异超过 `3mm`，进入暂停报警状态，等待人工确认。

不间断控制需要硬件支持。若要求断电期间也继续控制，需增加 24V UPS 或电池备份，并同时给 UM244、STM32、传感器供电。只有 STM32 备用电源而驱动器断电时，系统无法移动框篮。

## 时间基准设计

当前版本不增加 RTC 模块，也不接备用电池。固件使用 SysTick 或定时器维护上电后的毫秒计数，并由此派生秒、分钟、运行天数和打盹调度。

时间规则：

| 项目         | 规则                                                |
| ------------ | --------------------------------------------------- |
| 运行天数     | 只统计累计上电运行时间，`86400s` 记为 1 天          |
| 每日下降进度 | 只在设备上电且自动模式允许运行时累计                |
| 断电期间     | 不计时、不补偿、不追赶                              |
| 打盹间隔     | 上电或恢复自动后重新从当前时刻开始计时              |
| OLED 显示    | 显示“上电累计运行天数/当前日进度”，不是自然日历时间 |

若后续需要按真实日期、每天固定时间或断电期间继续计时，应增加 RTC 模块或启用 STM32 RTC 并提供备用电池。届时需要重新定义运行天数、每日下降进度和断电恢复策略。

## 开机自检流程

系统上电后必须先执行自检。是否自动恢复运行由断电前状态和自检结果共同决定。

开机自检步骤：

1. 初始化 GPIO、软件 I2C、OLED、蜂鸣器、限位输入、步进控制和 Flash 参数模块。
2. `PA0` 先置高电平关闭蜂鸣器，避免上电误鸣叫。
3. OLED 显示自检页面。
4. 蜂鸣器短鸣一次，确认报警外设可用。
5. 读取 Flash 参数；若参数无效，载入默认参数并提示用户。
6. 等待传感器稳定 10 秒。
7. 读取三颗 WF5805F，确认 I2C 通信正常。
8. 读取四个限位输入，确认限位组合没有物理矛盾。
9. 计算 `tank_depth_mm` 和 `basket_depth_mm`，检查是否处于合理物理范围。
10. 检查目标水深、每日变浅量、单次打盹脉冲数、手动速度等参数是否在允许范围。
11. 自检通过后根据断电恢复策略进入自动恢复或暂停状态。
12. 自检失败时进入自检失败报警状态，停止运动并等待维护处理。

Flash 参数无效时使用默认值：

| 参数           |  默认值 |
| -------------- | ------: |
| 初始目标水深   |   100mm |
| 最终目标水深   |    10mm |
| 每日变浅量     | 1mm/day |
| 单次打盹脉冲数 | 8 pulse |
| 手动速度       |   1mm/s |

## 首次安装与投运流程

首次安装时不要直接放入幼鱼运行。必须先完成空载和带水验证，确认电气、传感器、限位和机械方向均正确。

投运步骤：

1. 只给 STM32、OLED、传感器和蜂鸣器供电，暂不使能 UM244 输出。
2. 确认 24V、24V 转 5V 降压输出和最小系统板 3.3V 电源极性、电压正常，确认 STM32 与传感器供电共地关系正确。
3. 确认 OLED 正常显示自检页面，蜂鸣器开机短鸣一次。
4. 确认三颗 WF5805F 都能读取，空气参考读数稳定，鱼缸和框篮传感器入水后水深变化方向正确。
5. 手动触发四个限位开关，确认 OLED 显示对应限位，且低有效逻辑正确。
6. 暂不接电机，先测量或观察 `STEP`、`DIR`、`MF` 控制信号逻辑。
7. 接入电机，UM244 电流先设为 2.5A，在无负载或轻负载下低速点动，确认两侧电机方向一致。
8. 若两侧电机方向相反，交换其中一台电机同一相的两根线后重新测试。
9. 安装框篮后进入维护模式，执行低速手动上升/下降，确认上下限位都能阻止对应方向运动。
10. 执行机械回零流程，建立 `basket_position_mm = 0` 参考点。
11. 三颗传感器同处空气中执行维护菜单 `CAL AIR`，等待滤波稳定后确认 `BAS/TNK` 接近 `0.0mm`；再把 `P_tank/P_basket` 放回水中，确认水深随浸入深度增加。
12. 先不放鱼运行 24 小时，观察打盹动作、温升、噪声、I2C 稳定性和水深趋势。
13. 验证稳定后，才允许进入正式自动适应流程。

## 日常维护要求

系统投入使用后需要定期检查，避免传感器封装、机械阻力或线缆问题逐渐累积为安全风险。

维护建议：

| 周期           | 检查项                                                                                          |
| -------------- | ----------------------------------------------------------------------------------------------- |
| 每天           | OLED 是否有报警、框篮水深是否合理、鱼缸整体水位是否在安全范围内                                 |
| 每周           | 检查传感器线缆、防水封装、框篮是否倾斜、丝杆是否有异物                                          |
| 每月           | 检查丝杆润滑、限位开关固定、电机和 UM244 温升、24V/5V/3.3V 电源接线，确认 5V 降压模块端子无松动 |
| 每次换水后     | 重新观察 `tank_depth_mm` 和 `basket_depth_mm` 是否稳定，确认无水位突变报警                      |
| 每次拆装框篮后 | 进入维护模式重新检查限位和机械位置，必要时重新回零                                              |

任何明显进水、封装鼓包、传感器读数漂移、左右电机不同步或框篮卡滞，都应先退出自动模式，进入维护排查。

## OLED 与按键功能

OLED 默认显示：

- 当前框篮实际水深
- 当前目标水深
- 运行天数
- 模式：自动、暂停、手动上升、手动下降
- 自动打盹模式参数：每日变浅量、单次脉冲数、下次唤醒倒计时
- 手动点动方向和位置状态
- 今日已累计位移
- 限位和传感器状态
- 错误码和蜂鸣器静音状态

按键建议：

| 按键   | 功能                 |
| ------ | -------------------- |
| `PB1`  | 菜单/数值减/手动下降 |
| `PB11` | 菜单/数值加/手动上升 |
| `PB10` | 暂停/确认/报警静音   |
| `PB0`  | 页面/参数项切换      |

手动移动时仍执行限位保护。自动模式下目标水深按设定曲线变化，默认允许误差为 ±1mm。

按键事件定义：

| 事件         | 判定                                           |
| ------------ | ---------------------------------------------- |
| 短按         | 按下并松开，稳定持续 `25-1000ms`               |
| 长按         | 按下稳定持续 `1000ms` 以上                     |
| 维护模式长按 | `PB10` 按下稳定持续 `3000ms` 以上              |
| 手动点动     | `PB1` 或 `PB11` 长按期间持续移动，松手立即停止 |

按键判定必须先经过 25ms 稳定确认。短按有效时间为 `25-1000ms`，普通长按为 `1000ms` 以上，维护模式长按为 `PB10` 按下 `3000ms` 以上。

OLED 页面建议：

| 页面       | 显示内容                                           | 主要操作                                |
| ---------- | -------------------------------------------------- | --------------------------------------- |
| 主页面     | 框篮水深、目标水深、运行天数、模式、下次打盹倒计时 | `PB10` 暂停/继续，`PB0` 切换页面        |
| 传感器页面 | 三颗传感器压力、水深、I2C 状态、失败计数           | `PB0` 切换页面                          |
| 限位页面   | 四个限位输入、左右一致性、当前位置可信度           | `PB0` 切换页面                          |
| 参数页面   | 初始目标、最终目标、每日变浅量、单次打盹脉冲数     | `PB1/PB11` 修改，`PB10` 确认            |
| 手动页面   | 手动速度、当前位置、限位状态                       | `PB1` 长按下降，`PB11` 长按上升         |
| 报警页面   | 错误码、错误原因、蜂鸣器静音状态                   | `PB10` 静音；故障消除后 `PB10` 确认清除 |
| 维护页面   | 校准、回零、电机释放、调试读数                     | 进入后才允许维护操作                    |

## OLED 显示文字与页面规格

OLED 使用当前 0.96 寸 128x64 屏和 8x16 ASCII 字库，固件页面按 4 行、每行 16 个 ASCII 字符设计。默认不显示中文，避免后续代码生成时引入中文字库、字模占用和编码问题。所有页面刷新必须由主循环状态机按时间戳调度，禁止用 `Delay` 等待显示。

通用显示规则：

| 项目       | 规则                                                              |
| ---------- | ----------------------------------------------------------------- |
| 字符集     | 仅使用 ASCII 可见字符                                             |
| 页面尺寸   | 4 行 x 16 字符；不足 16 字符的行需要用空格清尾                    |
| 水深单位   | 显示为 `mm`，默认 1 位小数，例如 `100.0mm`                        |
| 压力单位   | 显示为 `hPa`，默认 2 位小数，例如 `1013.25hPa`                    |
| 时间单位   | 倒计时小于 1 小时显示 `014m24s`，大于等于 1 小时显示 `02h15m`     |
| 脉冲单位   | 显示为 `pls`                                                      |
| 布尔状态   | 正常/有效显示 `OK`，失败显示 `FAIL`，触发显示 `1`，未触发显示 `0` |
| 无效数据   | 数值用 `--.-mm`、`----.--hPa` 或 `---` 占位                       |
| 多个错误   | OLED 优先显示最高优先级严重故障；其他错误在第 3 行显示 `+N MORE`  |
| 蜂鸣器静音 | `MUTE:Y` 表示已静音，`MUTE:N` 表示未静音                          |

主页面模板：

```text
B:100.0 T:099.0
D001 AUTO RUN
NXT 014m24s
P008 ERR-- M-N
```

| 行  | 字段来源                                                | 异常显示                                          |
| --- | ------------------------------------------------------- | ------------------------------------------------- |
| 1   | `basket_depth_mm_x10`、`target_depth_mm_x10`            | 任一水深无效时对应字段显示 `--.-`                 |
| 2   | `run_days`、`app_mode`                                  | 模式显示 `AUTO`、`PAUS`、`MANU`、`MAINT`、`FAULT` |
| 3   | `next_nap_remaining_s`                                  | 暂停/故障/维护时显示 `NXT --m--s`                 |
| 4   | `today_done_pulses`、最高优先级错误摘要、`buzzer_muted` | 无错误显示 `ERR--`；有错误显示 `ERR!`             |

自检页面模板：

```text
SELF TEST
WAIT SENSOR 10s
I2C A- B- C-
LIM ----
```

| 行  | 字段来源                  | 异常显示                        |
| --- | ------------------------- | ------------------------------- |
| 1   | 固定标题                  | 自检失败后显示 `SELF TEST FAIL` |
| 2   | 传感器稳定等待倒计时      | 等待结束后显示 `CHECK DONE`     |
| 3   | 三条 WF5805F I2C 总线状态 | `AOK/BOK/COK` 或 `AFL/BFL/CFL`  |
| 4   | 四个限位输入原始/滤波状态 | 限位组合异常显示 `LIM ERR`      |

传感器页面模板：

```text
AIR 1013.25hPa
BAS 0100.0mm
TNK 0350.0mm
I2C A0 B0 C0
```

| 行  | 字段来源                  | 异常显示                                                             |
| --- | ------------------------- | -------------------------------------------------------------------- |
| 1   | `P_air.pressure_hpa_x100` | 空气参考失败显示 `AIR FAIL`                                          |
| 2   | `basket_depth_mm_x10`     | 框篮传感器失败显示 `BAS FAIL`，水深越界显示 `BAS LOW!` 或 `BAS HIGH` |
| 3   | `tank_depth_mm_x10`       | 鱼缸传感器失败显示 `TNK FAIL`，水深越界显示 `TNK LOW!` 或 `TNK HIGH` |
| 4   | I2C-A/B/C 恢复失败计数    | 正常显示 `I2C A0 B0 C0`；对应计数持续增长表示该总线近期恢复失败      |

限位页面模板：

```text
LU0 LD0 RU0 RD0
UP OK  DN OK
POS 050.0mm OK
LIM ERR:NONE
```

| 行  | 字段来源                                   | 异常显示                                                                      |
| --- | ------------------------------------------ | ----------------------------------------------------------------------------- |
| 1   | 四个限位滤波状态，`1` 为触发，`0` 为未触发 | 读数不可用显示 `LU- LD- RU- RD-`                                              |
| 2   | 上升/下降方向是否被限位阻止                | 允许显示 `UP OK` / `DN OK`；被阻止显示 `UP BLK` / `DN BLK`                    |
| 3   | `basket_position_mm_x10`、位置可信标志     | 位置可信显示 `POS xxx.xmm OK`；不可信显示 `POS xxx.xmm UN` 或 `POS --.-mm UN` |
| 4   | 限位错误摘要                               | 显示 `LIM ERR:NONE`、`LIM ERR:UPPER`、`LIM ERR:LOWER` 或 `LIM ERR:MISMT`      |

参数页面模板。参数页面采用一页编辑一个参数的方式，`PB1` 减小，`PB11` 增大，`PB10` 确认，`PB0` 取消或切换页面：

```text
P1 INIT DEPTH
VAL 100.0mm
RNG 005-100mm
PB1- PB11+ OK
```

| 参数页 | 第 1 行标题      | 第 2 行字段来源                            | 第 3 行范围     |
| ------ | ---------------- | ------------------------------------------ | --------------- |
| P1     | `P1 INIT DEPTH`  | `initial_target_depth_mm_x10`              | `RNG 005-100mm` |
| P2     | `P2 FINAL DEPTH` | `final_target_depth_mm_x10`                | `RNG 005-100mm` |
| P3     | `P3 DAILY RATE`  | `daily_shallow_mm_x10`，显示 `VAL 1.0mm/d` | `RNG 0.0-3.0`   |
| P4     | `P4 NAP PULSE`   | `nap_pulses`，显示 `VAL 008pls`            | `RNG 001-024`   |
| P5     | `P5 MAN SPEED`   | 固定 `VAL 1.0mm/s`                         | `READ ONLY`     |

参数异常显示规则：

```text
PARAM ERROR
OUT OF RANGE
KEEP OLD VALUE
PB10 CONFIRM
```

手动页面模板：

```text
MANUAL STOP
DEP B:100.0mm
POS 050.0mm
PB1 DN PB11 UP
```

| 行  | 字段来源                 | 异常显示                                                    |
| --- | ------------------------ | ----------------------------------------------------------- |
| 1   | 手动状态                 | 按住 `PB1` 显示 `MANUAL DOWN`；按住 `PB11` 显示 `MANUAL UP` |
| 2   | `basket_depth_mm_x10`    | 无效显示 `DEP B:--.-mm`                                     |
| 3   | `basket_position_mm_x10` | 位置不可信显示 `POS --.-mm`                                 |
| 4   | 操作提示                 | 方向限位触发时显示 `LIMIT BLOCKED`                          |

报警页面模板：

```text
FAULT TANK LOW
E_TANK_LOW
+0 MORE MUTE:N
PB10 MUTE/CLR
```

| 行  | 字段来源                                   | 异常显示                                                    |
| --- | ------------------------------------------ | ----------------------------------------------------------- |
| 1   | 最高优先级错误的短原因                     | 无错误显示 `ALARM NONE`                                     |
| 2   | 最高优先级错误码，超过 16 字符时使用短别名 | 无错误显示 `NO ERROR`                                       |
| 3   | 其他错误数量、蜂鸣器静音状态               | 无其他错误显示 `+0 MORE`                                    |
| 4   | 当前可执行操作                             | 故障未解除时显示 `PB10 MUTE`，故障已解除时显示 `PB10 CLEAR` |

错误码短别名建议：

| 错误码                 | OLED 短原因   |
| ---------------------- | ------------- |
| `E_SENSOR_AIR_FAIL`    | `AIR FAIL`    |
| `E_SENSOR_BASKET_FAIL` | `BAS FAIL`    |
| `E_SENSOR_TANK_FAIL`   | `TNK FAIL`    |
| `E_I2C_A_FAIL`         | `I2C-A FAIL`  |
| `E_I2C_B_FAIL`         | `I2C-B FAIL`  |
| `E_I2C_C_FAIL`         | `I2C-C FAIL`  |
| `E_TANK_LOW`           | `TANK LOW`    |
| `E_TANK_HIGH`          | `TANK HIGH`   |
| `E_BASKET_LOW`         | `BASKET LOW`  |
| `E_BASKET_HIGH`        | `BASKET HIGH` |
| `E_WATER_JUMP`         | `WATER JUMP`  |
| `E_PRESSURE_PHYSICAL`  | `PRESS PHYS`  |
| `E_UPPER_LIMIT`        | `UP LIMIT`    |
| `E_LOWER_LIMIT`        | `DN LIMIT`    |
| `E_LIMIT_MISMATCH`     | `LIM MISMT`   |
| `E_STALL`              | `STALL`       |
| `E_DEPTH_TRACKING`     | `DEPTH`       |
| `W_PARAM_DEFAULT`      | `PARAM DEF`   |

维护页面模板。维护模式采用菜单页和确认页两类页面：

```text
MAINT MENU 1/4
1 CAL AIR
2 HOME ZERO
PB10 OK PB0 BK
```

```text
CONFIRM HOME
MOTOR WILL MOVE
PB10 YES
PB0 NO
```

| 页面         | 字段来源                                            | 异常显示                                                                                                              |
| ------------ | --------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 维护菜单     | 当前菜单页、可执行维护项                            | 有严重故障时不显示自动恢复项                                                                                          |
| 空气校准     | `BAS/TNK` 相对 `AIR` 的零点偏移                     | 任一路传感器失败或滤波状态无效时拒绝保存                                                                              |
| 回零确认     | 位置可信标志、限位状态                              | 限位异常显示 `LIM ERR DENY`                                                                                           |
| 电机释放确认 | 当前模式、故障状态                                  | 自动模式禁止释放，显示 `AUTO DENY`                                                                                    |
| 调试读数     | 电机保持/释放、位置可信原因、最近回零状态或失败原因 | `POS NOHOME/HOMING/MFREL/INTR/LIMMIS/STALL/RANGE`；回零失败显示 `HOME DN LIMIT`、`HOME LIM MISMT`、`HOME TANK LOW` 等 |

数据不可用与异常占位统一规则：

| 情况               | 显示                                                                   |
| ------------------ | ---------------------------------------------------------------------- |
| 传感器启动等待     | `WAIT`                                                                 |
| 单个传感器连续失败 | `AIR FAIL`、`BAS FAIL`、`TNK FAIL`                                     |
| I2C 总线恢复失败   | `I2C-A FAIL`、`I2C-B FAIL`、`I2C-C FAIL`                               |
| 水深无效           | `--.-mm`                                                               |
| 压力无效           | `----.--hPa`                                                           |
| 位置不可信         | 限位/普通位置行使用 `UN` 表示位置不可信；维护调试页显示具体 `POS` 原因 |
| 参数越界           | `OUT OF RANGE`                                                         |
| 限位阻止运动       | `LIMIT BLOCKED`                                                        |
| 维护操作被拒绝     | `DENY`                                                                 |

参数修改规则：

- 目标水深只能在 `5-100mm` 范围内设置。
- 每日变浅量只能在 `0-3mm/day` 范围内设置。
- 单次打盹脉冲数只能在 `1-24 pulse` 范围内设置，默认 `8 pulse`。
- 菜单按键调整会把数值夹紧在合法边界；保存层仍会拒绝非法参数组合。若每日变浅量和单次打盹脉冲数导致理论打盹间隔小于 `5min`，OLED 显示 `PARAM ERROR` 并保持旧值，例如 `3.0mm/day` 需要至少 `9 pulse`。
- 手动速度当前只有一种档位：`1mm/s`，不提供多档切换或独立测速菜单。
- 保存接口拒绝非法记录时，OLED 显示错误提示，蜂鸣器短鸣一次。
- 参数修改成功后立即保存到 Flash，但必须受 Flash 写入策略限制。

## 维护模式与人工操作

维护模式用于三传感器同空气校准、手动点动、机械回零、电机保持/释放和调试读数等人工维护操作。维护模式不能绕过限位保护，也不能绕过严重故障后直接恢复自动运行。若传感器/I2C 或水位类旧告警已经锁存但当前根因已恢复，维护回零允许继续建立可信位置；该动作不清除锁存告警，恢复自动运行仍需走报警页清除流程。

进入维护模式：

| 操作                            | 行为               |
| ------------------------------- | ------------------ |
| 正常/暂停页面下长按 `PB10` 3 秒 | 进入维护模式确认页 |
| 确认页中短按 `PB10`             | 确认进入维护模式   |
| 确认页中短按 `PB0`              | 取消并返回上一页面 |

维护模式允许的操作：

| 功能                         | 是否允许             | 说明                                                   |
| ---------------------------- | -------------------- | ------------------------------------------------------ |
| 手动上升/下降                | 允许                 | 长按移动，松手立即停止                                 |
| 手动多档速度                 | 不提供               | 当前 v1 手动速度固定为 `1mm/s`                         |
| 报警蜂鸣器静音               | 允许                 | 在报警页执行；只静音，不清除故障状态                   |
| 压力传感器校准               | 允许                 | 三颗传感器同空气，保存 `BAS/TNK` 相对 `AIR` 的零点偏移 |
| 电机释放 `MF`                | 允许，但必须二次确认 | 用于维护；释放后不允许自动运动                         |
| 普通手动释放电机             | 不允许               | 只有维护模式可进入释放确认流程                         |
| 忽略水位类报警进行手动维护   | 允许                 | 仅维护模式下允许；不允许恢复自动运行                   |
| 忽略限位                     | 不允许               | 任何模式都不能绕过上下限位                             |
| 忽略左右限位不一致           | 不允许               | 该状态下禁止运动                                       |
| 忽略严重传感器故障并自动运行 | 不允许               | 只能维护检查，不能自动运行                             |

退出维护模式：

| 操作                       | 行为                                                                                     |
| -------------------------- | ---------------------------------------------------------------------------------------- |
| 维护模式下长按 `PB10` 3 秒 | 重新保持电机，清除 `E_MOTOR_RELEASED` 锁存，退出维护并回到暂停；若仍有其他故障则进入报警 |
| 触发严重故障               | 停止运动，保持维护模式并报警                                                             |
| 重新上电                   | 不自动恢复维护模式，先执行开机自检                                                       |

维护模式安全底线：

- 上限位触发时，禁止继续上升。
- 下限位触发时，禁止继续下降。
- 左右同方向限位不一致时，禁止全部运动。
- 水位类报警在真实根因仍存在时不能清除；实际水深恢复安全范围后，可在报警页人工确认清除，但不能直接恢复自动模式。
- 手动移动必须是长按动作，松手立即停止。
- 进入电机释放前，OLED 必须显示确认提示，蜂鸣器短鸣提示风险。
- 电机释放后，系统必须标记当前位置可信度降低；重新使能电机并人工确认后才能恢复自动，长按退出维护只允许顺带重新保持电机，不恢复位置可信。
- 维护模式下不执行自动打盹位移，不累计自动下降进度。

## 机械零点与回零流程

固件需要同时维护机械位置和水深读数。机械位置用于行程限制、断电恢复可信度和卡滞判断；水深读数用于目标控制和安全报警。

位置定义：

| 项目                     | 定义                                                       |
| ------------------------ | ---------------------------------------------------------- |
| `basket_position_mm`     | 框篮机械位置，单位 mm                                      |
| `basket_position_mm = 0` | 框篮最低位置，框篮内可活动水深最大                         |
| 正方向                   | 框篮向上移动，框篮内可活动水深变浅                         |
| 最大允许位置             | `100mm`，对应框篮最大行程                                  |
| 位置可信度               | 成功回零后为可信；电机释放、严重卡滞、限位矛盾后变为不可信 |

回零只允许在维护模式中人工执行，不在普通开机流程中自动回零，避免设备上电后意外运动。

回零流程：

1. 进入维护模式，OLED 显示回零确认页。
2. 用户确认后，系统先复查限位、阻塞故障和电机保持状态；安全预检通过后标记并保存位置不可信，再以手动速度 `1mm/s` 或更低的回零速度向下移动。
3. 任一下限位触发后立即停止下降。
4. 反向上升 `2mm` 释放下限位。
5. 再次低速下降，任一下限位触发即停止。
6. 再反向上升 `2mm` 退离下限位；机械 0 点仍为下限位触发点，退离后的当前位置记录为约 `+2.0mm`。
7. 清除位置不可信标志，将可信位置立即保存到 Flash。

若回零安全预检失败且没有进入实际回零运动，不因本次尝试额外保存 `HOMING_STARTED` 不可信标记；进入回零运动后失败仍保持位置不可信。成功回零后会立即保存机械 0 点和退离下限位后的可信当前位置，不等待 1 小时运行状态保存。

回零安全规则：

- 回零过程中上限位触发、上限位左右不一致、当前传感器/I2C 连续失败、水位仍异常或用户取消操作时立即停止。
- 已恢复的传感器/I2C 或水位旧锁存不阻塞维护回零，但回零不会自动清除这些锁存故障，自动运行仍需人工确认清除；`E_WATER_JUMP` 旧锁存需要鱼缸水位趋势样本有效、稳定至少 `15s`，且鱼缸水位变化速度不超过 `10mm/min`。
- `E_WATER_JUMP` 趋势样本不足或鱼缸水位仍突变时，维护回零显示 `HOME WATER` 并拒绝或中止。
- 回零向下搜索下限位时，任一侧下限位先触发视为预期停止条件；随后必须退离 `2mm`，若仍不能释放下限位则报下限位故障。
- 普通手动/自动运动中，左右同方向限位不一致仍进入限位不一致故障。
- 未完成回零时允许手动维护点动，但不允许恢复自动运行。
- 若电机释放 `MF` 被执行过，必须重新回零或人工确认当前位置可信后才能恢复自动运行。

## 水位与传感器异常判定

系统需要把水位异常和传感器异常明确区分，便于 OLED 显示错误原因。

水位异常：

| 异常           | 条件                                                                     | 行为                         |
| -------------- | ------------------------------------------------------------------------ | ---------------------------- |
| 鱼缸低水位     | `tank_depth_mm < 250mm` 约 10s 稳定确认；忽略明显跳变和 I2C-C 刚恢复样本 | 严重故障，停止自动下降并报警 |
| 鱼缸高水位异常 | 高水位由机械溢流/漏水限位保证；`TNK > 450mm` 仅作为诊断读数               | 不再主动锁存 `E_TANK_HIGH`   |
| 框篮低水位     | `basket_depth_mm < 5mm`                                                  | 严重故障，停止自动下降并报警 |
| 框篮高水位异常 | `basket_depth_mm > 120mm`                                                | 严重故障，停止自动运动并报警 |
| 目标水深越界   | 目标值小于 5mm 或大于 100mm                                              | 拒绝参数，保持原值并报警提示 |
| 水位突变       | 非可跟随突变或趋势确认后的危险掉水超过 30mm/min                          | 严重故障，停止自动运动并报警 |

传感器与 I2C 异常：

| 异常              | 条件                                              | 行为                         |
| ----------------- | ------------------------------------------------- | ---------------------------- |
| 单次 I2C 读数失败 | 任一传感器一次读取失败                            | 记录失败并重试               |
| 传感器连续失败    | 同一传感器连续 5 次读取失败                       | 严重故障，停止自动运动并报警 |
| I2C 恢复失败      | 同一总线重初始化 5 次仍失败                       | 严重故障，停止自动运动并报警 |
| 压力差物理异常    | 扣除对应空气零点后的框篮或鱼缸差压水深小于 `-2mm` | 严重故障，停止自动运动并报警 |

低水位报警允许按 `PB10` 静音蜂鸣器，但静音不清除故障状态。报警后若需要移动，只允许进入维护模式进行人工长按点动。

## 错误码表

固件需要使用固定错误码，便于 OLED 显示、日志记录和后续代码排查。错误码分为一般警告和严重故障；严重故障必须停止自动运动。

| 错误码                 | 级别 | 触发条件                                                                                      | 恢复条件                                               |
| ---------------------- | ---- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------ |
| `E_NONE`               | 正常 | 无错误                                                                                        | 无需处理                                               |
| `W_PARAM_DEFAULT`      | 警告 | Flash 参数无效，已载入默认值                                                                  | 用户确认后清除                                         |
| `W_PARAM_REJECTED`     | 警告 | 用户设置参数越界                                                                              | 返回合法参数后清除                                     |
| `E_SENSOR_AIR_FAIL`    | 严重 | 空气参考传感器连续 5 次读取失败                                                               | 通信恢复并人工确认                                     |
| `E_SENSOR_TANK_FAIL`   | 严重 | 鱼缸底部传感器连续 5 次读取失败                                                               | 通信恢复并人工确认                                     |
| `E_SENSOR_BASKET_FAIL` | 严重 | 框篮底部传感器连续 5 次读取失败                                                               | 通信恢复并人工确认                                     |
| `E_I2C_A_FAIL`         | 严重 | I2C-A 重初始化 5 次仍失败                                                                     | 总线恢复并人工确认                                     |
| `E_I2C_B_FAIL`         | 严重 | I2C-B 重初始化 5 次仍失败                                                                     | 总线恢复并人工确认                                     |
| `E_I2C_C_FAIL`         | 严重 | I2C-C 重初始化 5 次仍失败                                                                     | 总线恢复并人工确认                                     |
| `E_TANK_LOW`           | 严重 | `tank_depth_mm < 250mm` 约 10s 稳定确认；判定去除单个 TNK 极值并忽略明显跳变/I2C-C 刚恢复样本 | 水深恢复并人工确认                                     |
| `E_TANK_HIGH`          | 严重 | 兼容占位；当前固件不再因 `tank_depth_mm > 450mm` 主动锁存                                 | 旧锁存可人工确认清除                                   |
| `E_BASKET_LOW`         | 严重 | `basket_depth_mm < 5mm`                                                                       | 水深恢复正常并人工确认                                 |
| `E_BASKET_HIGH`        | 严重 | `basket_depth_mm > 120mm`                                                                     | 水深恢复正常并人工确认                                 |
| `E_WATER_JUMP`         | 严重 | 趋势确认后的危险掉水、非可跟随突变或水深变化过快                                              | 鱼缸水位趋势恢复并人工确认                             |
| `E_PRESSURE_PHYSICAL`  | 严重 | 压力差对应水深小于 `-2mm`                                                                     | 读数恢复正常并人工确认                                 |
| `E_UPPER_LIMIT`        | 严重 | 上升方向触发上限位                                                                            | 停止上升，退出限位后人工确认                           |
| `E_LOWER_LIMIT`        | 严重 | 下降方向触发下限位                                                                            | 停止下降，退出限位后人工确认                           |
| `E_LIMIT_MISMATCH`     | 严重 | 左右同方向限位状态不一致                                                                      | 排查机械同步和限位后人工确认                           |
| `E_POSITION_UNTRUSTED` | 严重 | 位置不可信且请求自动运行                                                                      | 可人工确认清除提示；自动运行前仍必须回零或恢复位置可信 |
| `E_STALL`              | 严重 | 连续 8 次卡滞嫌疑                                                                             | 维护排查后人工确认                                     |
| `E_DEPTH_TRACKING`     | 严重 | 目标硬误差超过 `±5mm`、低频闭环连续修正失败、重启水深差异超过 `3mm` 或水深等待超时            | 读数恢复正常并人工确认                                 |
| `E_SELF_TEST_FAIL`     | 严重 | 开机自检未通过                                                                                | 故障消除并人工确认                                     |
| `E_MOTOR_RELEASED`     | 严重 | 电机释放后请求自动运行                                                                        | 重新使能并确认位置可信                                 |

错误处理规则：

- 同一时间可以存在多个错误标志，OLED 优先显示最高优先级严重故障。
- 蜂鸣器静音只影响声音，不清除错误码。
- 一般警告可以在原因消失后由用户确认清除。
- 严重故障必须满足恢复条件并人工确认后才能清除。
- 清除严重故障后默认进入暂停状态，不直接进入自动运行；用户再次确认后才运行。

## 报警输出策略

报警同时通过 OLED、有源蜂鸣器和 LED2 表现。OLED 用于显示具体错误原因，蜂鸣器用于提醒操作者及时处理。当前固件版本中 LED1 只作为主循环心跳，每 `5000ms` 短亮约 `200ms` 一次；LED2 作为错误/警告锁存指示，只要存在任意已锁存错误或警告就常亮，无异常时熄灭。

| 状态          | OLED                       | LED                     | 蜂鸣器                     |
| ------------- | -------------------------- | ----------------------- | -------------------------- |
| 正常自动运行  | 显示水深、目标和打盹倒计时 | LED1 5s 短闪，LED2 熄灭 | 关闭                       |
| 暂停/手动模式 | 显示当前模式               | LED1 5s 短闪，LED2 熄灭 | 关闭                       |
| 一般警告      | 显示警告码和原因           | LED1 5s 短闪，LED2 常亮 | 短鸣 3 次后关闭            |
| 严重故障      | 显示故障码和人工确认提示   | LED1 5s 短闪，LED2 常亮 | 间歇鸣叫，按 `PB10` 可静音 |

严重故障包括传感器连续读数失败、上下限位异常、左右同方向限位不一致、I2C 总线连续恢复失败、压力差明显超出物理范围、鱼缸低水位、框篮低水位、危险水位突变、卡滞保护触发、低频闭环修正失败和水深目标硬超差。蜂鸣器静音只关闭声音，不清除故障状态；故障必须在原因消除并人工确认后才能恢复自动控制。

## 固件状态机

固件主循环应以状态机为中心，不把控制逻辑分散在中断或显示代码中。

| 状态                | 含义               | 允许动作                                     | 退出条件                                                                     |
| ------------------- | ------------------ | -------------------------------------------- | ---------------------------------------------------------------------------- |
| `APP_SELF_TEST`     | 开机自检           | 初始化外设、读 Flash、读传感器和限位         | 自检通过进入恢复判断；失败进入 `APP_FAULT`                                   |
| `APP_PAUSED`        | 暂停待确认         | 显示状态、允许进菜单/维护、允许静音          | 用户确认开始自动；进入维护；出现故障                                         |
| `APP_AUTO_RUN`      | 自动运行判定       | 计算目标水深和是否需要打盹                   | 进入 `APP_NAP_WAIT`、`APP_NAP_MOVE`、暂停或故障                              |
| `APP_NAP_WAIT`      | 等待下一次打盹     | 读水深、读限位、倒计时                       | 时间到进入 `APP_NAP_MOVE`；暂停或故障                                        |
| `APP_NAP_MOVE`      | 执行一次微小位移   | 输出有限数量 STEP 脉冲                       | 脉冲完成回到 `APP_NAP_WAIT`；限位/故障立即停止                               |
| `APP_MANUAL`        | 普通手动点动       | 长按上升/下降，松手停                        | 松手、暂停、进入维护或故障                                                   |
| `APP_MAINTENANCE`   | 维护模式           | 校准、回零、手动点动、调试读数、电机释放确认 | 用户退出回到 `APP_PAUSED`；严重故障保持本状态并报警                          |
| `APP_MOTOR_RELEASE` | 电机释放维护子状态 | 使能 `MF` 释放电机                           | 用户重新使能电机后回维护模式，或长按退出时保持电机后回暂停；位置标记为不可信 |
| `APP_FAULT`         | 故障状态           | 停止运动、报警、允许静音和进入维护           | 故障原因消除并人工确认后进入 `APP_PAUSED`                                    |

状态机硬性规则：

- 任何状态下出现严重故障，都必须立即停止 STEP 脉冲。
- 限位保护优先级高于手动和自动命令。
- `APP_NAP_MOVE` 只允许执行有限脉冲，不允许无限循环输出 STEP。
- `APP_MOTOR_RELEASE` 后不允许直接进入自动运行。
- 自动恢复只允许从开机自检后的恢复判断进入，不能从故障清除后直接进入。

## 控制与安全行为

主控制目标是让 `basket_depth_mm` 跟随目标曲线。自动模式默认采用打盹式计划位移，每天累计让框篮相对水面上升 1mm，使框篮内水深缓慢变浅。控制器根据框篮实际水深、目标水深、当天剩余计划脉冲数和限位状态决定是否执行下一次微小位移：

- 框篮实际水深大于目标水深：框篮上升，使鱼可活动水深变浅
- 框篮实际水深小于目标水深：框篮下降，使鱼可活动水深变深
- 自动下降适应期内，不连续运行电机；只在打盹唤醒窗口内执行 `1 pulse` 或 `8 pulse` 的极小位移
- 误差在 ±1mm 内：按每日计划继续打盹累加，不做额外追调
- 误差在 1-2mm：只观察，不动作，并阻止每日打盹继续向上变浅
- 误差达到 2mm：低频闭环小步修正；上升修正计入当日变浅额度，下降补深不计入
- 误差超过 5mm 或连续 5 次修正仍不能回到启动死区内：进入 `E_DEPTH_TRACKING`
- 每次位移后立即停止 STEP 脉冲，回到静止保持状态

异常处理：

- 任意传感器读数失败：停止自动移动，报警
- 扣除框篮空气零点后的差压水深为负或明显超出鱼缸物理范围：停止自动移动，报警
- 限位触发方向与运动方向冲突：停止对应方向运动
- 左右同方向限位不一致：停止全部运动，报警
- I2C 长时间无响应：重新初始化总线；连续失败后进入暂停报警

## 运动控制细节

运动控制模块只负责方向、脉冲数量、脉冲频率和限位急停，不直接决定目标曲线。

运动参数：

| 项目             |                                         数值 |
| ---------------- | -------------------------------------------: |
| 丝杆导程         |                                      2mm/rev |
| UM244 细分       |                               1600 pulse/rev |
| 位移换算         |                                 800 pulse/mm |
| 自动默认单次位移 |                             8 pulse = 0.01mm |
| 自动最大单次位移 |                            24 pulse = 0.03mm |
| 自动脉冲频率     |                   默认 800Hz，可降级为 400Hz |
| 手动速度         |                          1mm/s = 800 pulse/s |
| 回零速度         | 默认 0.5mm/s = 400 pulse/s，最高不超过 1mm/s |

方向定义：

| 命令 | 机械动作     | 水深变化               |
| ---- | ------------ | ---------------------- |
| 上升 | 框篮向上移动 | `basket_depth_mm` 变小 |
| 下降 | 框篮向下移动 | `basket_depth_mm` 变大 |

`DIR` 引脚的具体高低电平必须在首次投运时实测确认。若实测发现方向相反，应优先通过配置宏反转方向逻辑，不直接改动控制算法。

脉冲规则：

- `STEP` 使用定时器或严格计时函数产生固定数量脉冲。
- 后续代码需要以 UM244 手册为准满足最小高电平、低电平和方向建立时间。
- `DIR` 改变后至少等待 `5ms` 再输出 STEP，最后一个 STEP 后至少保持 `5ms`。
- 自动打盹每次只发送计划脉冲数，发送完成立即停止。
- `APP_NAP_MOVE` 执行期间设置忙锁，禁止重复触发新的打盹动作。
- 每个脉冲发送前都应检查当前方向对应的限位状态。
- 若运动中触发方向对应限位，立即停止本次运动并置故障。
- 自动模式不使用加减速；手动和回零速度较高，建议后续代码预留简单斜坡接口，但第一版可以固定频率运行。

## 卡滞与过载保护

当前版本不增加 24V 电流检测模块，因此 STM32 无法直接测量电机电流，也无法直接判断驱动器内部过流。UM244 的硬件过流、短路等故障由驱动器自身保护；固件侧采用“累计位移后的水深趋势检查”作为卡滞/过载的软件保护。

卡滞检测规则：

| 参数             | 数值 |
| ---------------- | ---: |
| 最小检测累计位移 |  1mm |
| 期望最小水深变化 |  1mm |
| 连续卡滞次数     | 8 次 |

检测逻辑：

1. 自动模式中累计上升位移达到 1mm 后，比较 `basket_depth_mm` 的变化趋势。
2. 若计划让框篮上升 1mm，则框篮实际可活动水深应至少减少约 1mm。
3. 若变化方向不对，或变化量明显不足，则记录一次卡滞嫌疑。
4. 连续 8 次卡滞嫌疑后，进入严重故障报警，停止自动运动。
5. 卡滞报警后只能进入维护模式排查，不能自动继续打盹。

由于水压传感器存在噪声和封装误差，卡滞检测不在每次 `8 pulse` 微小位移后立即判断，只在累计位移达到 1mm 后判断趋势。

## Flash 存储结构

STM32F103C8T6 标称 Flash 为 64KB，页大小通常为 1KB。当前方案使用最后两个 Flash 页保存配置和恢复状态，采用 A/B 双页循环日志，避免单页写入中断导致参数完全丢失，并减少普通运行状态擦写次数。

Flash 预留：

| 项目           |                            地址 | 大小 | 用途                            |
| -------------- | ------------------------------: | ---: | ------------------------------- |
| 参数页 A       |                    `0x0800F800` |  1KB | 配置/状态日志 A                 |
| 参数页 B       |                    `0x0800FC00` |  1KB | 配置/状态日志 B                 |
| Keil IROM 建议 | `0x08000000`，大小 `0x0000F800` | 62KB | 固件代码区，避开最后 2KB 参数页 |

保存记录建议字段：

| 字段                       | 说明                                                                 |
| -------------------------- | -------------------------------------------------------------------- |
| `magic`                    | 固定标识，例如 `0x414C4654`                                          |
| `version`                  | 结构版本，当前为 `1`                                                 |
| `seq`                      | 递增序号，选择最新有效记录                                           |
| `crc16`                    | 覆盖记录主体的 CRC16                                                 |
| `initial_target_mm_x10`    | 初始目标水深                                                         |
| `final_target_mm_x10`      | 最终目标水深                                                         |
| `daily_shallow_mm_x10`     | 每日变浅量                                                           |
| `nap_pulses`               | 单次打盹脉冲数                                                       |
| `total_run_seconds`        | 累计上电运行秒数                                                     |
| `today_run_seconds`        | 当前运行日已累计上电秒数                                             |
| `today_pulses_done`        | 当前运行日已执行自动变浅脉冲数；只统计每日打盹向上和低频闭环向上修正 |
| `basket_position_pulses`   | 估算机械位置，单位 pulse                                             |
| `position_trusted`         | 位置可信标志                                                         |
| `last_app_state`           | 断电前应用状态                                                       |
| `last_tank_depth_mm_x10`   | 最近一次有效鱼缸水深                                                 |
| `last_basket_depth_mm_x10` | 最近一次有效框篮水深                                                 |
| `air_offset_hpa_x100`      | 维护空气零点校准打包值，复用字段保存 `zero_offset_basket/tank`       |
| `buzzer_muted`             | 蜂鸣器静音状态                                                       |

写入策略：

- 参数被用户修改并确认后，由状态机合并当前运行态并单次保存。
- 运行状态最多每 1 小时保存一次。
- 回零安全预检通过并进入回零流程后保存位置不可信；回零成功会立即保存机械 0 点和退离下限位后的可信当前位置。
- 完成累计 1mm 卡滞趋势检查后保存一次状态。
- 进入暂停、故障、维护、电机释放前保存一次状态。
- 不允许每个 `1 pulse` 或每个 `8 pulse` 打盹后都擦写 Flash。
- 当前受限闭环不新增 Flash 字段，`version` 保持 `1`；DROP 采用保守 v1 恢复策略，首次接管时复用 `PARAM_STORE_APP_NAP_MOVE` 保存恢复标记，DROP 小步过程中不反复写 Flash。重启读到该标记时不会自动运行，需人工检查水位/位置；用户确认并重新启动自动后再强制保存正常自动状态以清除旧标记。
- 每个 1KB 页按 `sizeof(ParamStore_Record_t)` 划分固定槽位，记录结构仍为完整 v1 全量记录，不新增字段。
- 写入时优先追加到当前有效页的下一个空槽；当前页满、有损坏槽或无法安全追加时，擦除另一页并写入槽位 0。
- 读取时扫描 A/B 两页全部槽位，同时检查 `magic`、`version`、`seq`、`crc16` 和字段范围，选择 `seq` 最大的有效记录；两页都无效则使用默认参数并置 `W_PARAM_DEFAULT`。

## 实现边界

本设计只固化硬件接线和系统行为。后续代码实现应优先添加在 `user/` 和 `Hardware/` 下，保持 Keil C 编译兼容，不修改 `Objects/`、`Listings/`、`DebugConfig/`、`Library/`、`start/`、`system/` 和 `*.uvguix` 文件。

代码和工程约束：

- 使用 STM32 标准外设库，不使用 HAL。
- Keil 工程已启用 C99，但代码仍采用保守 Keil C 风格。
- 不使用动态内存、变长数组和复合字面量。
- 中断服务函数必须短小，只做计数、置标志或紧急停机，不放复杂控制逻辑。
- 每个模块使用一个 `.c/.h` 文件，硬件引脚和系统常量集中放在板级配置头文件中。
- 新增代码优先放在 `Hardware/`，主流程只改 `user/main.c`。
- 不修改已有 `Hardware/*.c` 文件，除非为了集成新模块或修复明确 bug。
- 后续如果需要修改 Keil 工程文件以加入新源文件，只修改 `project1.uvprojx`，不修改 `*.uvguix`。
- 代码必须能在 STM32F103C8T6 上编译并运行。

后续实现可分为以下模块：

- 参数范围与默认值配置
- 可配置引脚的软件 I2C 驱动
- WF5805F 读数与压力换算
- 校准参数管理
- 开机自检流程
- OLED 显示页面
- 按键扫描与菜单
- 维护模式与人工操作状态机
- 有源蜂鸣器报警输出
- UM244 步进脉冲控制
- 自动打盹运动调度
- 水位异常和传感器健康监测
- 卡滞与过载软件保护
- 限位输入和安全状态机
- Flash 参数保存
- 自动水深曲线控制

## 已知风险

- WF5805F 自制防水封装是系统最大硬件风险。封装必须保证水压可传递到传感器，同时电路不进水。
- 三颗绝对压力传感器需要统一标定零点和比例系数，否则会出现固定偏差。
- 两个电机并联一个驱动器时，电流分配和同步性不如两个驱动器独立控制。当前方案可用于原型验证，若出现不同步、丢步或温升异常，应升级为两个 UM244 驱动器共用 STEP/DIR。
- I2C 线缆进入水下环境后容易受电机噪声和线缆长度影响。三颗 WF5805F 因固定地址相同，已分别放在 I2C-A、I2C-B、I2C-C 三条压力传感器软件 I2C 总线上；OLED 独立使用 `PB8/PB9`，不与压力传感器共用总线。调试时仍需验证线缆长度、上拉阻值和屏蔽接地。
- 当前版本不增加 24V 电流检测模块，无法直接检测真实电机过载；固件只能通过累计位移后的水深趋势做卡滞判断。
- 当前版本不增加 RTC 模块，运行天数和每日下降进度只按上电运行时间计算；断电期间不会计时。
