# 鱼缸框篮自动升降控制装置使用说明书

本文面向第一次接触本设备的操作者。内容按当前项目文档和当前代码整理，只说明已经在固件中接入的功能；和规格文档存在差异的地方列在最后的“需确认事项”。

## 1. 设备用途

本设备控制鱼缸内框篮上下移动，让框篮内的可活动水深按设定速度慢慢变浅。它控制的是框篮高度，不是整缸水位。

默认目标：

| 项目         |         当前默认值 |
| ------------ | -----------------: |
| 初始目标水深 |            100.0mm |
| 最终目标水深 |             10.0mm |
| 每日变浅量   |          1.0mm/day |
| 自动单次打盹 | 8 pulse，约 0.01mm |
| 自动脉冲频率 |              800Hz |
| 手动速度     |            1.0mm/s |

当前版本没有 RTC，运行天数和每日进度只按上电运行时间累计。断电期间不计时、不运动、不补偿。

## 2. 按键说明

| 按键             | 短按作用                                               | 长按作用                                                 |
| ---------------- | ------------------------------------------------------ | -------------------------------------------------------- |
| `PB0` 页面/取消  | 切换 OLED 页面；确认页取消                             | 当前代码未使用普通长按                                   |
| `PB1` 减小/下降  | 参数减小；维护菜单向前选择                             | 在手动页按住约 1 秒后下降，松手停止                      |
| `PB11` 增加/上升 | 参数增加；维护菜单向后选择                             | 在手动页按住约 1 秒后上升，松手停止                      |
| `PB10` 暂停/确认 | 主页面启动/暂停；保存参数；报警静音/确认；维护菜单确认 | 按住约 3 秒进入维护确认；在维护中再次长按约 3 秒退出维护 |

按键为低电平按下。短按约为 25ms 到 1s；`PB1/PB11` 长按到 1s 后开始手动点动；`PB10` 长按到 3s 后触发维护入口或退出维护。

## 3. OLED 页面

OLED 使用 4 行英文/数字显示。`PB0` 用于切换页面。

| 页面     | OLED 主要内容                                                                                                                                                                      | 说明                                         |
| -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| 主页面   | `B:` 当前框篮水深，`T:` 当前目标水深，`Dxxx` 运行日，`AUTO/PAUS/FAULT/MAINT/MANU/SELF` 模式，`NXT` 下次打盹倒计时，`Pxxx` 今日已执行脉冲，`ERR` 错误提示，`M-Y/M-N` 蜂鸣器是否静音 | 日常查看页面                                 |
| 自检页   | `SELF TEST`、传感器稳定倒计时、`I2C A/B/C` 状态、四个限位状态                                                                                                                      | 上电自检时自动显示                           |
| 传感器页 | `AIR` 空气压力，`BAS` 框篮水深，`TNK` 鱼缸水深，`I2C A/B/C` 恢复失败计数                                                                                                           | 用于确认三颗压力传感器                       |
| 限位页   | `LU/LD/RU/RD` 四个限位，`UP OK/BLK`，`DN OK/BLK`，`POS` 位置，`LIM ERR`                                                                                                            | 用于确认限位和位置可信                       |
| 参数页   | `P1 INIT DEPTH`、`P2 FINAL DEPTH`、`P3 DAILY RATE`、`P4 NAP PULSE`、`P5 MAN SPEED`                                                                                                 | `PB1-`、`PB11+` 调整，`PB10` 保存；`P5` 只读 |
| 手动页   | `MANUAL STOP/UP/DOWN/LIMIT`，框篮水深，位置，`PB1 DN PB11 UP`                                                                                                                      | 长按 `PB1/PB11` 点动                         |
| 报警页   | `FAULT`、错误简称、错误码、其他错误数量、静音状态                                                                                                                                  | `PB10` 静音或确认                            |
| 维护页   | `1 CAL AIR`、`2 HOME ZERO`、`3 MOTOR HOLD/RELEASE`、`4 DEBUG READ`                                                                                                                 | 维护操作入口                                 |

参数页注意：改完参数后请先按 `PB10` 保存。若直接按 `PB0` 切到下一项，当前未保存修改会被放弃。

## 4. 日常操作

### 4.1 开机

1. 上电后蜂鸣器短鸣一次。
2. OLED 进入 `SELF TEST`，等待约 10 秒让传感器稳定。
3. 自检通过后，系统按断电前状态处理：
   - 上次是自动相关状态，且位置可信、水深差异不超过 3mm、无严重故障，会自动恢复自动运行。
   - 上次是暂停，进入暂停。
   - 上次是故障、维护或电机释放，不自动运行。
   - Flash 参数无效时使用默认值，并显示参数默认警告。

### 4.2 启动自动运行

1. 确认 OLED 主页面不是 `FAULT`。
2. 确认已完成回零，限位页 `POS` 显示可信。
3. 确认框篮和鱼缸水深在安全范围内。
4. 在主页面显示 `PAUS` 时，短按 `PB10`。
5. OLED 进入 `AUTO`，并显示下次打盹倒计时。

自动运行中，固件会按设定间隔做极小的向上打盹运动，使框篮内水深逐步变浅。每次运动前后都会检查传感器和限位；若出现严重故障，立即停止自动运行。

### 4.3 暂停自动运行

在主页面显示 `AUTO` 时短按 `PB10`。系统进入暂停并保存运行状态。暂停后不会继续打盹。

### 4.4 手动升降

1. 按 `PB0` 切到手动页 `MANUAL`。
2. 长按 `PB11` 约 1 秒后开始上升。
3. 长按 `PB1` 约 1 秒后开始下降。
4. 松开按键即停止。

限位保护始终有效：上限位触发时不能继续上升，下限位触发时不能继续下降，左右同方向限位不一致时停止全部运动。报警后需要移动时，只允许在维护模式中点动。

### 4.5 进入和退出维护模式

进入维护：

1. 长按 `PB10` 约 3 秒。
2. OLED 出现 `CONFIRM MAINT`。
3. 按 `PB10` 确认进入维护，按 `PB0` 取消。

维护菜单：

| 菜单           | 操作                            |
| -------------- | ------------------------------- |
| `1 CAL AIR`    | 按 `PB10` 记录当前空气参考压力  |
| `2 HOME ZERO`  | 按 `PB10` 进入回零确认          |
| `3 MOTOR HOLD` | 按 `PB10` 进入电机保持/释放确认 |
| `4 DEBUG READ` | 按 `PB10` 查看维护调试信息      |

用 `PB1/PB11` 选择维护菜单项。退出维护模式请再次长按 `PB10` 约 3 秒；退出后回到暂停状态。

### 4.6 回零

回零用于建立机械最低点，自动运行前必须有可信位置。

1. 进入维护模式。
2. 选择 `2 HOME ZERO`。
3. 按 `PB10`，进入确认页。
4. 再按 `PB10` 确认；按 `PB0` 取消。
5. 设备会低速下降寻找下限位；若已经在下限位，会先上升约 1mm 释放限位，再第二次下降靠近下限位。
6. 第二次触发下限位后，当前位置记为 0mm，位置变为可信。

回零过程中不能绕过限位。若出现上限位、左右限位不一致、传感器严重故障、水位严重异常或电机释放故障，回零会停止并进入故障。

### 4.7 电机释放和重新保持

电机默认保持，不会在自动模式释放。

释放电机只允许在维护模式中操作：

1. 进入维护模式。
2. 选择 `3 MOTOR HOLD`。
3. 按 `PB10`，进入确认页。
4. 再按 `PB10` 执行释放或重新保持。

执行释放后，当前位置会变为不可信，并产生 `E_MOTOR_RELEASED`。重新保持电机后，仍需要重新回零或确认位置可信，才能恢复自动运行。

### 4.8 报警静音和清除

严重故障时蜂鸣器会报警，OLED 显示故障码。

1. 第一次短按 `PB10`：只让蜂鸣器静音，不清除故障。
2. 排除故障原因后，再短按 `PB10`：固件会复查限位、传感器、水深、位置、电机释放等条件。
3. 若根因已经消失，故障被清除并进入暂停。
4. 若根因仍存在，保持故障状态，只保持静音。

一般警告，例如参数默认或参数越界提示，可以在原因处理后用 `PB10` 确认清除。

## 5. 参数设置

按 `PB0` 切到参数页。`PB1` 减小，`PB11` 增加，`PB10` 保存。

| 参数页           | 含义           | 当前保存范围                       |
| ---------------- | -------------- | ---------------------------------- |
| `P1 INIT DEPTH`  | 初始目标水深   | 8.0 到 100.0mm                     |
| `P2 FINAL DEPTH` | 最终目标水深   | 8.0 到 100.0mm，且不能大于初始目标 |
| `P3 DAILY RATE`  | 每日变浅量     | 0.0 到 2.0mm/day                   |
| `P4 NAP PULSE`   | 单次打盹脉冲数 | 1 到 16 pulse                      |
| `P5 MAN SPEED`   | 手动速度       | 当前固定 1.0mm/s，只读             |

保存越界参数会被拒绝，OLED 显示 `PARAM ERROR`，并保持原来的有效值。

## 6. 断电重启行为

当前代码的断电恢复规则如下：

1. 重启后一定先自检。
2. 断电期间不计时、不运动、不补偿错过的下降量。
3. 如果上次是自动运行相关状态，并且位置可信、传感器和限位正常、当前水深与断电前记录差异不超过 3mm，才会自动恢复。
4. 恢复后下一次打盹从当前时间重新排程，不会追赶断电期间错过的打盹。
5. 如果上次是暂停，重启后仍暂停。
6. 如果上次是故障、维护或电机释放，重启后不会自动运行。

## 7. 安全限制

请按以下规则使用设备：

- 不能绕过限位器。任意上限位触发会禁止上升，任意下限位触发会禁止下降，左右同方向限位不一致会停止全部运动。
- 严重故障不能靠静音清除。静音只关闭蜂鸣器，故障状态仍然锁存。
- 自动模式默认不释放电机。释放电机只允许在维护模式中确认执行。
- 自动运行需要位置可信。第一次装机、回零失败、电机释放、限位不一致、卡滞后，都不能直接自动运行。
- 传感器连续失败、I2C 恢复连续失败、水位过低/过高、水位突变、压力物理异常、卡滞、目标跟踪超差都会停止自动运行。
- 自动打盹每次只输出有限脉冲，不会连续低速拖动电机。
- Flash 不会在每次 1 pulse 或 8 pulse 打盹后擦写；运行状态按节流保存，关键状态切换时强制保存。

## 8. 第一次上电调试流程

第一次调试必须空载、无鱼、有人看守，旁边保留能快速切断 24V 电源的方法。

### 8.1 上电前检查

1. 先不要接电机，或保持 UM244 禁用。
2. 机械上支撑好框篮，防止意外下落。
3. 确认 24V 线不会碰到 STM32、OLED、按键、WF5805F 信号线。
4. 确认 24V、5V、3.3V 电源极性正确。
5. 5V 只接 STM32 最小系统板 `5V` 输入、UM244 `PU+/DR+/MF+` 和信号光耦输出侧。
6. OLED、三颗 WF5805F、蜂鸣器、按键、光耦输入侧按 3.3V 逻辑供电。
7. 限位器黑线不能直接接 STM32，必须经过 24V NPN 输入型光耦。

### 8.2 界面和按键

1. 烧录当前固件。
2. 上电后确认 OLED 显示 `SELF TEST`。
3. 确认蜂鸣器短鸣一次后关闭。
4. 分别按 `PB0/PB1/PB10/PB11`，确认页面或数值有反应。
5. 确认 LED 低电平点亮，高电平熄灭。

### 8.3 压力传感器

1. 确认 OLED 独占 `PB8/PB9`。
2. `P_air` 接 `PA1/PA2`，放在空气中。
3. `P_basket` 接 `PB6/PB7`，安装在框篮底部。
4. `P_tank` 接 `PA8/PA9`，安装在鱼缸底部。
5. 切到传感器页，确认 `AIR/BAS/TNK` 读数有效。
6. 把水下传感器逐步浸入水中，确认水深数值随浸入深度增加。

### 8.4 限位器

1. 切到限位页。
2. 逐个手动触发左上、左下、右上、右下限位。
3. 未触发时应显示 0，触发时应显示 1。
4. 确认上限位会显示 `UP BLK`，下限位会显示 `DN BLK`。
5. 故意制造左右同方向不一致时，应进入限位不一致故障；测试后必须排除原因再清除。

### 8.5 UM244 信号，不带电机

1. 保持电机不接或驱动器禁用。
2. 确认 `PA3/PA4/PA5` 分别接 STEP/DIR/MF 光耦输入。
3. 确认 UM244 `PU+/DR+/MF+` 接 5V。
4. 确认 STM32 没有直接接 UM244 的 5V 输入端。
5. 测量 `PU-/DR-/MF-`，有效低电平应为 0 到 0.5V。
6. 确认 MF 默认保持电机，不释放。

### 8.6 电机方向

1. UM244 电流先按 2.5A 起步。
2. 细分设置为 1600 pulse/rev。
3. 进入维护模式或手动页，短时间低速点动。
4. 确认两个电机同向同步。
5. 如果一个电机方向相反，只交换该电机同一绕组的两根线。
6. 确认软件“上升”会让框篮上升，框篮水深变浅；“下降”会让框篮下降，框篮水深变深。

### 8.7 机械限位和回零

1. 装上空框篮，不放鱼。
2. 进入维护模式。
3. 手动上升，确认上限位能停止上升。
4. 手动下降，确认下限位能停止下降。
5. 执行回零，确认二次触发下限位后位置变为 0mm 且可信。

### 8.8 空载试运行

1. 设置目标水深、每日变浅量和单次打盹脉冲。
2. 不放鱼，自动运行至少 24 小时。
3. 观察打盹间隔是否符合设置。
4. 检查电机和 UM244 温升是否正常。
5. 检查 I2C 是否有连续失败。
6. 做一次受控断电重启，确认不会补偿断电期间错过的运动。
7. 确认报警静音不会清除故障。

## 9. 常见故障提示

| OLED 错误                                  | 含义                                         | 操作建议                         |
| ------------------------------------------ | -------------------------------------------- | -------------------------------- |
| `AIR FAIL` / `TNK FAIL` / `BAS FAIL`       | 对应压力传感器连续读取失败                   | 检查 3.3V、GND、I2C 线和防水封装 |
| `I2C-A FAIL` / `I2C-B FAIL` / `I2C-C FAIL` | 对应 I2C 总线恢复失败                        | 检查总线上拉、短路、线缆和传感器 |
| `TANK LOW/HIGH`                            | 鱼缸水深过低或过高                           | 调整实际水位，确认传感器位置     |
| `BASKET LOW/HIGH`                          | 框篮水深过低或过高                           | 暂停使用，检查框篮位置和传感器   |
| `UP LIMIT` / `DN LIMIT`                    | 运动方向触发限位                             | 排查机械位置，不能强行继续       |
| `LIM MISMT`                                | 左右同方向限位不一致                         | 检查两侧机械同步、限位器和接线   |
| `POS UNTR`                                 | 位置不可信                                   | 执行回零                         |
| `STALL`                                    | 运动后水深变化不符合预期                     | 检查电机、丝杆、卡滞和传感器     |
| `DEPTH`                                    | 目标跟踪超差、重启水深差异过大或水深等待超时 | 检查水深、传感器稳定性和目标设置 |
| `MOTOR REL`                                | 电机释放过                                   | 重新保持电机并回零               |

## 10. 当前未作为已实现功能使用

- 没有 RTC，断电时间不会被计算。
- 没有 24V 电流检测模块，无法直接检测电机电流。
- 没有两个驱动器独立同步控制；当前是两个电机并联到一个 UM244。
- 不会在每次上电时自动回零。
- 不会在断电期间移动，也不会在重启后追赶断电期间错过的下降量。

## 11. 需确认事项

| 事项                 | 当前代码行为                                                                                                                    | 需要确认                                                               |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 自动纠偏方向         | 当前自动打盹只执行向上变浅。若框篮水深相对目标超过 ±1mm，会进入 `E_DEPTH_TRACKING`，不会自动向下补深。                          | 是否接受当前“只向上、超差报警”的自动策略，还是后续需要实现双向自动纠偏 |
| 空气校准             | 维护菜单 `CAL AIR` 会保存当前空气压力值，但当前水深计算仍使用实时 `P_sensor - P_air` 差压，没有把该保存值作为校准偏移参与计算。 | 是否把该项称为“空气参考记录”，或后续让它真正参与校准                   |
| 维护页 `PB0 BK` 字样 | 确认页中 `PB0` 是取消；普通维护菜单中 `PB0` 实际会切换页面，退出维护需要再次长按 `PB10` 约 3 秒。                               | 是否需要后续修改 OLED 字样，避免操作者误以为短按 `PB0` 会退出维护      |
| 严重故障清除提示     | 严重故障静音后 OLED 可能显示 `FAULT LATCHED`，代码允许在根因消除后再次按 `PB10` 受控清除。                                      | 是否需要后续把 OLED 提示改成更明确的“双击确认/原因消除后再确认”        |

