# 鱼缸框篮自动升降控制装置用户手册

本文按当前固件源码和项目规格整理，面向第一次使用本设备的操作者。OLED 只显示英文、数字和符号，本手册集中解释这些显示内容。

## 1. 用途与安全底线

本设备控制鱼缸内框篮上下移动，让框篮内可活动水深按设定速度缓慢变浅。它控制的是框篮高度，不控制整缸水位。

当前默认参数：

| 项目         |                                                    默认值 |
| ------------ | --------------------------------------------------------: |
| 初始目标水深 |                                                 `100.0mm` |
| 最终目标水深 |                                                  `10.0mm` |
| 每日变浅量   |                                               `1.0mm/day` |
| 单次自动打盹 |                                    `8 pulse`，约 `0.01mm` |
| 自动脉冲频率 |                                                   `800Hz` |
| 手动速度     |                                                 `1.0mm/s` |
| 低频闭环修正 |                     每 `3min` 检查一次，普通单步 `0.25mm` |
| 快速掉水跟随 | 鱼缸 `30s` 趋势下降 `5-30mm/min` 且框篮变浅时分块下降跟随 |

必须遵守的安全底线：

- 自动运行前必须完成回零，并让位置变为可信。
- 上限位触发时禁止上升，下限位触发时禁止下降，左右同方向限位不一致时禁止全部运动。
- 严重故障会停止自动运动；蜂鸣器静音只关闭声音，不清除故障。
- 自动模式不释放电机。电机释放只允许在维护模式中二次确认执行。
- 当前版本没有 RTC，断电期间不计时、不运动、不补偿错过的打盹；保存的 `NAP_MOVE`/DROP 标记重启后也不会自动运行。

## 2. 按键速查

四个按键都是低电平按下。短按约为 `25ms` 到 `1s`；`PB1/PB11` 按住约 `1s` 后进入手动点动；`PB10` 按住约 `3s` 进入或退出维护模式。

| 按键             | 短按作用                                               | 长按作用                                         |
| ---------------- | ------------------------------------------------------ | ------------------------------------------------ |
| `PB0` 页面/取消  | 切换页面；确认页取消                                   | 当前普通长按未使用                               |
| `PB1` 减小/下降  | 参数减小；维护菜单向前选择                             | 手动页长按下降，松手停止                         |
| `PB11` 增加/上升 | 参数增加；维护菜单向后选择                             | 手动页长按上升，松手停止                         |
| `PB10` 暂停/确认 | 主页面启动/暂停；保存参数；报警静音/确认；维护菜单确认 | 长按 `3s` 进入维护确认；维护中长按 `3s` 退出维护 |

## 3. OLED 显示速查

OLED 是 `4 行 x 16 字符`。数值不可用时会显示 `--.-`、`----.--`、`--m--s` 或 `---`。LED1/LED2 只表示主循环心跳，不表示自动、暂停、故障或传感器状态。

### 3.1 页面切换顺序

短按 `PB0` 按下面顺序切换页面：

| 页面     | 主要用途                                             |
| -------- | ---------------------------------------------------- |
| 主页面   | 查看水深、目标、模式、下次打盹、错误和蜂鸣器静音状态 |
| 自检页   | 查看开机自检倒计时、I2C 初步状态和限位状态           |
| 传感器页 | 查看空气压力、框篮水深、鱼缸水深和 I2C 恢复失败次数  |
| 限位页   | 查看四个限位、上下方向是否被挡、位置和限位错误       |
| 参数页   | 修改初始水深、最终水深、每日变浅量、单次打盹脉冲     |
| 手动页   | 长按 `PB1/PB11` 手动下降/上升                        |
| 报警页   | 查看错误简称、错误码、其他错误数量和蜂鸣器静音状态   |
| 维护页   | 空气参考记录、回零、电机保持/释放、调试读数          |

### 3.2 所有英文显示说明

| 显示                        | 含义                                                                          |
| --------------------------- | ----------------------------------------------------------------------------- |
| `AUTO`                      | 自动运行或等待自动打盹                                                        |
| `TRK`                       | 自动模式中的低频闭环水深修正，普通偏差时小步上升或下降                        |
| `DROP`                      | 自动模式中的快速掉水跟随，水位稳定后进入暂停并等待人工确认                    |
| `PAUS`                      | 暂停，等待人工确认                                                            |
| `MANU`                      | 手动点动页面                                                                  |
| `MAINT`                     | 维护模式                                                                      |
| `FAULT`                     | 严重故障状态                                                                  |
| `SELF` / `SELF TEST`        | 开机自检                                                                      |
| `TEST`                      | 自检中的动作状态                                                              |
| `RUN`                       | 自动运行判定中                                                                |
| `WAIT`                      | 等待下一次打盹                                                                |
| `NAP`                       | 正在执行一次自动打盹微动                                                      |
| `JOG`                       | 手动点动                                                                      |
| `STOP`                      | 停止或故障停机                                                                |
| `IDLE`                      | 空闲保持                                                                      |
| `B:`                        | 框篮内当前水深，单位 `mm`                                                     |
| `T:`                        | 当前目标水深，单位 `mm`                                                       |
| `Dxxx`                      | 上电累计运行日的格式，`xxx` 是 3 位数字                                       |
| `D001`                      | 上电累计运行日，第 1 天显示 `D001`                                            |
| `NXT`                       | 下一次自动打盹倒计时                                                          |
| `CHECK WATER`               | 快速掉水跟随结束或保守恢复标记触发后提示检查真实水位/位置，确认前不会恢复自动 |
| `Pxxx`                      | 今日已完成自动脉冲数的格式，`xxx` 是 3 位数字                                 |
| `P008`                      | 今日已完成自动脉冲数，最大显示到 `P999`                                       |
| `ERR--`                     | 当前无错误                                                                    |
| `ERR!`                      | 当前存在警告或故障，需要看报警页                                              |
| `M-Y`                       | 蜂鸣器已静音                                                                  |
| `M-N`                       | 蜂鸣器未静音                                                                  |
| `UI DATA INVALID`           | 主页面没有收到有效状态数据                                                    |
| `WAIT SENSOR`               | 自检等待传感器数据稳定                                                        |
| `--s`                       | 自检倒计时秒数不可用                                                          |
| `I2C A- B- C-`              | 自检页的三条 I2C 初步状态，随后用 `0` 或 `X` 覆盖                             |
| `LIM`                       | 自检页限位状态汇总                                                            |
| `AIR`                       | 空气参考压力传感器 `P_air`                                                    |
| `BAS`                       | 框篮水深传感器 `P_basket` / 框篮水深                                          |
| `TNK`                       | 鱼缸水深传感器 `P_tank` / 鱼缸水深                                            |
| `hPa`                       | 压力单位                                                                      |
| `mm`                        | 水深或位置单位                                                                |
| `mm/d`                      | 每日变浅量单位                                                                |
| `pls`                       | 脉冲数单位                                                                    |
| `I2C A0 B0 C0`              | 三条压力传感器 I2C 总线恢复失败计数                                           |
| `LU0 LD0 RU0 RD0`           | 四个限位输入的完整显示格式                                                    |
| `LU` / `LD`                 | 左上限位 / 左下限位                                                           |
| `RU` / `RD`                 | 右上限位 / 右下限位                                                           |
| `0` / `1`                   | 限位未触发 / 已触发                                                           |
| `UP OK` / `UP BLK`          | 上升方向允许 / 上升方向被限位挡住                                             |
| `DN OK` / `DN BLK`          | 下降方向允许 / 下降方向被限位挡住                                             |
| `POS`                       | 机械位置，单位 `mm`                                                           |
| `OK`                        | 正常、有效或位置可信                                                          |
| `UN` / `UNTRUST`            | 位置不可信                                                                    |
| `LIM ERR:NONE`              | 当前无可显示的限位错误                                                        |
| `MISMT`                     | 左右同方向限位不一致                                                          |
| `UPPER` / `LOWER`           | 上限位 / 下限位相关错误                                                       |
| `PARAM ERROR`               | 参数保存或参数记录错误                                                        |
| `NO RECORD`                 | 参数记录不存在或不可用                                                        |
| `OUT OF RANGE`              | 参数越界或组合不合法                                                          |
| `KEEP OLD VALUE`            | 保留旧参数，不写入非法值                                                      |
| `P1 INIT DEPTH`             | 初始目标水深                                                                  |
| `P2 FINAL DEPTH`            | 最终目标水深                                                                  |
| `P3 DAILY RATE`             | 每日变浅量                                                                    |
| `P4 NAP PULSE`              | 单次自动打盹脉冲数                                                            |
| `P5 MAN SPEED`              | 手动速度，只读                                                                |
| `VAL`                       | 当前参数值                                                                    |
| `RNG`                       | 参数允许范围                                                                  |
| `RNG 005-100mm`             | 水深参数允许 `5.0` 到 `100.0mm`                                               |
| `RNG 0.0-3.0`               | 每日变浅量允许 `0.0` 到 `3.0mm/day`                                           |
| `RNG 001-024`               | 单次打盹允许 `1` 到 `24 pulse`                                                |
| `VAL 1.0mm/s`               | 手动速度固定为 `1.0mm/s`                                                      |
| `READ ONLY`                 | 只读，不可修改                                                                |
| `PB10 CONFIRM`              | 按 `PB10` 确认参数错误提示                                                    |
| `PB1- PB11+ OK`             | `PB1` 减小，`PB11` 增加，`PB10` 保存                                          |
| `PB1- PB11+ OK*`            | 参数已修改但还未保存                                                          |
| `MANUAL STOP`               | 手动页停止                                                                    |
| `MANUAL UP`                 | 手动上升                                                                      |
| `MANUAL DOWN`               | 手动下降                                                                      |
| `MANUAL LIMIT`              | 当前手动方向被限位禁止                                                        |
| `DEP B:`                    | 手动页框篮水深                                                                |
| `PB1 DN PB11 UP`            | `PB1` 下降，`PB11` 上升                                                       |
| `LIMIT BLOCKED`             | 当前方向被限位或限位不一致禁止                                                |
| `ALARM` / `ALM`             | 报警页含义；当前 OLED 实际显示 `ALARM NONE` 或 `FAULT ...`，不单独显示 `ALM`  |
| `ALARM NONE` / `NO ERROR`   | 当前无错误                                                                    |
| `+0 MORE` / `+0 MORE MUTE:` | 除当前显示的错误外，还有 0 个其他错误，并显示静音状态                         |
| `MUTE:Y` / `MUTE:N`         | 报警页静音状态：已静音 / 未静音                                               |
| `PB10 MUTE`                 | 按 `PB10` 静音蜂鸣器                                                          |
| `PB10 CLEAR`                | 按 `PB10` 确认清除可清除的警告或故障                                          |
| `FAULT LATCHED`             | 严重故障仍锁存，需先排除根因                                                  |
| `MAINT MENU`                | 维护菜单                                                                      |
| `1 CAL AIR`                 | 记录当前空气参考压力                                                          |
| `2 HOME ZERO`               | 回零，建立机械 0 点                                                           |
| `3 MOTOR HOLD`              | 电机保持/释放入口                                                             |
| `MOTOR HOLD/RELEASE`        | 本手册对 `3 MOTOR HOLD` 功能的概括写法，OLED 菜单项仍显示 `3 MOTOR HOLD`      |
| `4 DEBUG READ`              | 维护调试读数                                                                  |
| `REF`                       | 已记录的空气参考压力                                                          |
| `REF ----.--hPa`            | 空气参考压力不可用                                                            |
| `CONFIRM`                   | 二次确认页面                                                                  |
| `ACTION`                    | 通用确认动作名                                                                |
| `MAINT`                     | 确认进入维护模式                                                              |
| `HOME`                      | 确认执行回零                                                                  |
| `MF`                        | 电机释放/保持控制信号                                                         |
| `NO AUTO MOVE`              | 进入维护后不会自动打盹                                                        |
| `MOTOR WILL MOVE`           | 确认后电机会移动                                                              |
| `MOTOR MAY MOVE`            | 确认后电机可能移动                                                            |
| `MOTOR RELEASE`             | 确认后释放电机                                                                |
| `MOTOR HOLD`                | 确认后重新保持电机                                                            |
| `PB10 YES`                  | `PB10` 确认                                                                   |
| `PB0 NO`                    | `PB0` 取消                                                                    |
| `PB10 OK PB0 BK`            | `PB10` 确认；普通维护菜单里 `PB0` 实际是切到下一页，确认页里 `PB0` 是取消     |
| `MAINT DEBUG`               | 维护调试信息页                                                                |
| `MF RELEASED`               | 电机已释放                                                                    |
| `MF HOLD`                   | 电机保持中                                                                    |
| `POS TRUSTED`               | 位置可信                                                                      |
| `POS UNTRUST`               | 位置不可信                                                                    |
| `POS OK`                    | 维护菜单中位置可信的简写                                                      |
| `HOME BUSY`                 | 回零正在执行                                                                  |
| `HOME IDLE`                 | 回零未执行                                                                    |
| `PB10 TO HOLD`              | 按 `PB10` 重新保持电机                                                        |
| `PB10 RELEASE`              | 按 `PB10` 进入释放确认                                                        |

当前固件不在 OLED 上显示 `READY`、`PASS`、`NO WARN`、`WARN`、`SENSOR FAIL`、`LIMIT FAIL`、`PARA FAIL`、`PB10 CLR`、`HOLD?`、`REL?`。若旧资料出现这些写法，请以上表的当前显示为准。

### 3.3 错误简称和错误码

报警页第 1 行显示短原因，第 2 行显示错误码。两者含义如下：

| 短原因        | 错误码             | 含义                                                         |
| ------------- | ------------------ | ------------------------------------------------------------ |
| `NONE`        | `E_NONE`           | 无错误                                                       |
| `PARAM DEF`   | `W_PARAM_DEFAULT`  | Flash 参数无效，已载入默认值                                 |
| `PARAM ERR`   | `W_PARAM_REJECTED` | 参数越界或参数组合不合法                                     |
| `AIR FAIL`    | `E_SENSOR_AIR`     | 空气参考传感器连续读取失败                                   |
| `TNK FAIL`    | `E_SENSOR_TANK`    | 鱼缸水深传感器连续读取失败                                   |
| `BAS FAIL`    | `E_SENSOR_BAS`     | 框篮水深传感器连续读取失败                                   |
| `I2C-A FAIL`  | `E_I2C_A`          | 空气参考 I2C 总线恢复失败                                    |
| `I2C-B FAIL`  | `E_I2C_B`          | 框篮传感器 I2C 总线恢复失败                                  |
| `I2C-C FAIL`  | `E_I2C_C`          | 鱼缸传感器 I2C 总线恢复失败                                  |
| `TANK LOW`    | `E_TANK_LOW`       | 鱼缸水深过低                                                 |
| `TANK HIGH`   | `E_TANK_HIGH`      | 鱼缸水深过高                                                 |
| `BASKET LOW`  | `E_BASKET_LOW`     | 框篮水深过低                                                 |
| `BASKET HIGH` | `E_BASKET_HIGH`    | 框篮水深过高                                                 |
| `WATER JUMP`  | `E_WATER_JUMP`     | 危险掉水、不可跟随突变或水深变化过快                         |
| `PRESS PHYS`  | `E_PRESS_PHYS`     | 压力差不符合物理安装条件                                     |
| `UP LIMIT`    | `E_UPPER_LIMIT`    | 上升方向触发上限位                                           |
| `DN LIMIT`    | `E_LOWER_LIMIT`    | 下降方向触发下限位                                           |
| `LIM MISMT`   | `E_LIMIT_MISMT`    | 左右同方向限位不一致                                         |
| `POS UNTR`    | `E_POS_UNTRUST`    | 位置不可信，不能自动运行                                     |
| `STALL`       | `E_STALL`          | 运动后水深变化趋势不符合预期                                 |
| `DEPTH`       | `E_DEPTH_TRACK`    | 目标硬超差、低频修正失败、重启水深差异过大或等待新鲜水深超时 |
| `SELF FAIL`   | `E_SELF_TEST`      | 开机自检失败                                                 |
| `MOTOR REL`   | `E_MOTOR_REL`      | 电机释放后请求自动运行，或位置因释放变为不可信               |
| `UNKNOWN`     | `E_UNKNOWN`        | 未知错误码                                                   |

当前固件错误码以本表为准，不使用旧资料中的 `E_WATER_LOSS`、`E_SENSOR_FAIL`、`E_I2C_STUCK`、`E_LIMIT_TOP`、`E_LIMIT_BOTTOM`、`E_LIMIT_CONFLICT`、`E_PARAM_BAD`、`E_HOME_REQUIRED`、`W_NEED_HOME`、`W_DEPTH_OVER`、`W_AIR_FAIL`。

## 4. 蜂鸣器与报警

蜂鸣器接 `PA0`，低电平响，高电平关闭。

| 场景                           | 蜂鸣器行为                           |
| ------------------------------ | ------------------------------------ |
| 上电自检                       | 短鸣一次，确认蜂鸣器可控             |
| 正常自动、暂停、手动、维护空闲 | 关闭                                 |
| 参数保存失败                   | 短鸣提示，参数不会写入非法值         |
| 进入电机释放确认               | 短鸣提示风险                         |
| 一般警告                       | 通过报警页显示，`PB10` 可确认清除    |
| 严重故障                       | 未静音时蜂鸣器持续报警，自动运动停止 |

严重故障时 `PB10` 的规则：

1. 第一次短按 `PB10`：只静音蜂鸣器，OLED 会显示 `M-Y` 或 `MUTE:Y`。故障仍然锁存，自动运行不会恢复。
2. 排除故障根因后，再短按 `PB10`：固件复查限位、传感器/I2C、水深、位置和电机释放状态。
3. 根因已消失：清除故障并进入暂停 `PAUS`。
4. 根因仍存在：保持 `FAULT`，保持静音，不允许恢复自动。

不要把静音当成修复。静音只是不再响，必须先处理 OLED 显示的故障原因。

## 5. 日常操作

### 5.1 开机

1. 上电后蜂鸣器短鸣一次。
2. OLED 显示 `SELF TEST`，等待约 `10s` 让压力传感器稳定。
3. 自检通过后，固件按断电前状态处理：
   - 上次是普通自动等待状态，且位置可信、传感器和限位正常、水深差异不超过 `3mm`，才自动恢复。
   - 上次正在执行有限脉冲运动，或 Flash 保存了复用的 DROP 恢复标记时，不会继续半截运动，也不会自动恢复；OLED 显示 `CHECK WATER`，位置按不可信处理，需要人工检查水位和位置。
   - 上次处于快速掉水跟随时，不会自动继续 DROP；自检后进入暂停或故障，并提示人工检查水位。
   - 上次是暂停，仍进入暂停。
   - 上次是故障、维护或电机释放，不自动运行。
   - Flash 参数无效时载入默认值，并显示 `PARAM DEF`。

#### 5.1.1 断电重启和 `CHECK WATER` 人工确认

看到 `CHECK WATER`、`POS UNTR` 或 `FAULT` 时，不要反复短按 `PB10` 试图让系统直接恢复自动。先按下面流程确认：

1. 检查真实鱼缸水位、框篮内水深、三个压力传感器固定位置、传感器进水/堵塞情况、四个限位状态和机构是否卡住。
2. 如果是运行中 `DROP` 正常结束，主页面显示 `PAUS` 和 `CHECK WATER`，限位页没有 `POS UNTR`，报警页没有严重故障；确认掉水原因已经处理后，可回到主页面短按 `PB10` 重新进入 `AUTO`。
3. 如果是断电重启后出现 `CHECK WATER`，或报警页显示 `POS UNTR`，说明断电前可能处于 `NAP_MOVE` 或 DROP 保守恢复标记内。固件不会继续半截运动，也不会自动恢复。
4. `POS UNTR` 存在时，先长按 `PB10` 约 `3s` 进入维护确认，确认后选择 `2 HOME ZERO` 执行回零；等限位页或维护调试页显示位置可信后，再按报警页流程清除可清除故障。
5. 故障清除后系统回到 `PAUS`。再次确认水位、限位和机构正常，再在主页面短按 `PB10` 启动自动。

### 5.2 启动和暂停自动运行

启动自动运行：

1. 确认主页面不是 `FAULT`。
2. 确认限位页 `POS` 可信，没有 `UN` 或 `POS UNTR`。
3. 确认框篮和鱼缸水深在安全范围内。
4. 主页面显示 `PAUS` 时短按 `PB10`。
5. 主页面变为 `AUTO`，并显示 `NXT` 倒计时。

暂停自动运行：主页面显示 `AUTO` 时短按 `PB10`。暂停后不继续打盹，也不会继续低频修正或快速掉水跟随。

自动运行说明：

- `AUTO` 是普通自动等待或每日打盹。每日打盹只向上，让框篮水深按目标曲线慢慢变浅。
- `TRK` 是低频闭环修正。每约 `3min` 比较 `basket_depth - target_depth`：偏差在 `1.0-2.0mm` 时只观察，但会阻止每日打盹继续上升；偏深超过 `2.0mm` 时上升，偏浅超过 `2.0mm` 时下降；修正到 `1.0mm` 内停止。向上修正会计入今日变浅进度，并受当天剩余额度限制；向上额度不足时不会退回每日打盹。向下补深不计入今日进度。
- `DROP` 是快速掉水跟随。鱼缸水位以 `5-30mm/min` 下降并导致框篮水深同步变浅时，系统只允许框篮下降补深；如果鱼缸掉水但框篮没有按阈值变浅，视为不可跟随突变并报警。水位稳定、超时或达到单次距离限制后进入 `PAUS`，OLED 显示 `CHECK WATER`，需要人工确认水位原因后才能恢复自动。
- 普通闭环硬误差超过 `5.0mm`、连续修正失败、危险掉水超过 `30mm/min`、低水位、限位、传感器/I2C、位置不可信或电机释放都会进入故障。

今日进度 `Pxxx` 只统计“自动变浅脉冲”：每日打盹向上和 `TRK` 向上修正会计入，并共享当天剩余变浅额度；`TRK` 向下补深、`DROP` 跟随下降、手动点动和回零都不计入。

### 5.3 手动升降

1. 短按 `PB0` 切到 `MANUAL` 手动页。
2. 长按 `PB11` 约 `1s` 后上升。
3. 长按 `PB1` 约 `1s` 后下降。
4. 松开按键即停止。

限位保护始终有效。报警后若需要移动，只允许在维护模式中人工点动，且仍不能绕过限位。

## 6. 维护操作

### 6.1 进入和退出维护

进入维护：

1. 长按 `PB10` 约 `3s`。
2. OLED 显示 `CONFIRM MAINT` 和 `NO AUTO MOVE`。
3. 短按 `PB10` 确认，短按 `PB0` 取消。

退出维护：维护模式中再次长按 `PB10` 约 `3s`，退出后回到暂停。

### 6.2 维护菜单

| 菜单           | 操作                 | 说明                                                                               |
| -------------- | -------------------- | ---------------------------------------------------------------------------------- |
| `1 CAL AIR`    | 按 `PB10`            | 记录当前空气参考压力，用于维护显示；当前水深计算仍使用实时 `P_sensor - P_air` 差压 |
| `2 HOME ZERO`  | 按 `PB10` 后二次确认 | 执行回零，建立机械 0 点                                                            |
| `3 MOTOR HOLD` | 按 `PB10` 后二次确认 | 释放或重新保持电机；释放后位置变为不可信                                           |
| `4 DEBUG READ` | 按 `PB10`            | 查看 `MF`、位置可信和回零状态                                                      |

### 6.3 回零

回零用于建立机械最低点，自动运行前必须有可信位置。

1. 进入维护模式。
2. 选择 `2 HOME ZERO`。
3. 按 `PB10` 进入确认页。
4. 再按 `PB10` 确认，或按 `PB0` 取消。
5. 固件低速下降寻找下限位；若已经在下限位，会先上升约 `1mm` 释放限位，再第二次下降靠近下限位。
6. 第二次触发下限位后，当前位置记为 `0mm`，位置变为可信。

回零过程中不能绕过限位。若出现限位不一致、严重传感器故障、水位异常或电机释放故障，回零会停止并进入故障。

### 6.4 电机释放和重新保持

电机默认保持，不会在自动模式释放。

释放电机只允许在维护模式中执行：

1. 进入维护模式。
2. 选择 `3 MOTOR HOLD`。
3. 按 `PB10` 进入确认页。
4. 再按 `PB10` 执行释放或重新保持。

执行释放后，当前位置变为不可信，并产生 `E_MOTOR_REL`。重新保持电机后，仍需要回零或恢复位置可信，才能恢复自动运行。

## 7. 参数设置

短按 `PB0` 切到参数页。`PB1` 减小，`PB11` 增加，`PB10` 保存。

| 参数页           | 含义           | 范围和规则                                 |
| ---------------- | -------------- | ------------------------------------------ |
| `P1 INIT DEPTH`  | 初始目标水深   | `5.0` 到 `100.0mm`                         |
| `P2 FINAL DEPTH` | 最终目标水深   | `5.0` 到 `100.0mm`，且不能大于初始目标     |
| `P3 DAILY RATE`  | 每日变浅量     | `0.0` 到 `3.0mm/day`，还受最小打盹间隔限制 |
| `P4 NAP PULSE`   | 单次打盹脉冲数 | `1` 到 `24 pulse`                          |
| `P5 MAN SPEED`   | 手动速度       | 固定 `1.0mm/s`，只读                       |

参数页注意：

- 显示 `OK*` 表示已修改但未保存。
- 按 `PB10` 保存后，`OK*` 消失。
- 如果直接短按 `PB0` 切换参数项，未保存修改会被放弃。
- 如果每日变浅量和单次脉冲导致理论打盹间隔小于 `5min`，保存层会拒绝，OLED 显示 `PARAM ERROR` 和 `OUT OF RANGE`，旧值保留。例如 `3.0mm/day` 通常需要先把 `P4 NAP PULSE` 设为至少 `9 pulse`。

## 8. 首次上电调试

第一次调试必须空载、无鱼、有人看守，并保留快速切断 `24V` 电源的方法。

1. 电机先不接，或保持 UM244 禁用；机械上支撑好框篮。
2. 确认 `24V` 不会直接进入 STM32、OLED、按键、WF5805F 或蜂鸣器信号线。
3. 确认 `5V` 只接 STM32 最小系统板 `5V`、UM244 `PU+/DR+/MF+` 和信号光耦输出侧。
4. 确认 OLED、三颗 WF5805F、蜂鸣器、按键、光耦输入侧按 `3.3V` 逻辑供电。
5. 上电后确认 OLED 显示 `SELF TEST`，蜂鸣器短鸣一次后关闭。
6. 分别按 `PB0/PB1/PB10/PB11`，确认页面、参数或确认动作有反应。
7. 传感器页确认 `AIR/BAS/TNK` 读数有效，水下传感器浸入更深时水深数值增加。
8. 限位页逐个触发 `LU/LD/RU/RD`，未触发应为 `0`，触发应为 `1`。
9. 不带电机测 UM244 `PU-/DR-/MF-`，有效低电平应为 `0` 到 `0.5V`，确认 `MF` 默认保持电机。
10. 接电机后低速点动，确认两个电机同向同步，软件上升会让框篮上升且框篮水深变浅。
11. 空框篮执行限位测试和回零，确认下限位二次触发后 `POS` 变为可信。
12. 不放鱼自动运行至少 `24h`，观察打盹间隔、温升、I2C 稳定性和断电重启行为。

## 9. 故障处理

| OLED 显示                                  | 常见原因                                                 | 操作建议                                 |
| ------------------------------------------ | -------------------------------------------------------- | ---------------------------------------- |
| `AIR FAIL` / `TNK FAIL` / `BAS FAIL`       | 对应压力传感器连续读取失败                               | 查 `3.3V`、GND、I2C 线、防水封装和传感器 |
| `I2C-A FAIL` / `I2C-B FAIL` / `I2C-C FAIL` | 对应 I2C 总线恢复失败                                    | 查上拉、短路、线缆、接头和传感器         |
| `TANK LOW` / `TANK HIGH`                   | 鱼缸整体水深过低或过高                                   | 调整实际水位，确认 `P_tank` 位置         |
| `BASKET LOW` / `BASKET HIGH`               | 框篮水深过低或过高                                       | 停止使用，检查框篮位置和 `P_basket`      |
| `WATER JUMP`                               | 危险掉水、不可跟随突变或水深变化过快                     | 检查传感器固定、水流扰动和真实水位       |
| `PRESS PHYS`                               | 压力差不符合物理安装                                     | 检查传感器是否接反、进水或位置错误       |
| `UP LIMIT` / `DN LIMIT`                    | 运动方向触发限位                                         | 排查机械位置，不要强行继续               |
| `LIM MISMT`                                | 左右同方向限位不一致                                     | 查两侧机械同步、限位器和接线             |
| `POS UNTR`                                 | 位置不可信                                               | 执行回零或确认位置恢复可信               |
| `STALL`                                    | 运动后水深趋势不对                                       | 查电机、丝杆、卡滞、联轴和传感器         |
| `DEPTH`                                    | 目标硬超差、低频修正失败、重启水深差异过大或水深等待超时 | 查实际水深、传感器稳定性和目标参数       |
| `SELF FAIL`                                | 开机自检失败                                             | 根据报警页错误码逐项排查                 |
| `MOTOR REL`                                | 电机释放过或仍处于释放状态                               | 重新保持电机，并回零或恢复位置可信       |

故障处理顺序：

1. 看报警页第 1 行短原因和第 2 行错误码。
2. 按 `PB10` 静音蜂鸣器，但不要认为故障已清除。
3. 排除实际原因。
4. 再按 `PB10` 让固件复查并清除可清除的故障。
5. 清除后系统进入暂停，需要人工再次确认才会自动运行。

## 10. 当前限制

- 没有 RTC，断电时间不会被计算。
- 不会在断电期间移动，也不会在重启后追赶断电期间错过的打盹。
- 不会每次上电自动回零。
- 低频闭环只处理普通小偏差；硬误差、连续修正失败或危险掉水仍会报警，不会长期追调传感器漂移。
- 快速掉水跟随结束后默认暂停并显示 `CHECK WATER`，不会自动回到 `AUTO`；断电后读到 `NAP_MOVE`/DROP 保守标记也必须人工检查后才能重新启动。
- 没有 24V 电流检测模块，不能直接检测电机电流。
- 当前是两个电机并联到一个 UM244，不是双驱动器独立同步控制。
- 当前维护菜单没有独立蜂鸣器测试项；蜂鸣器行为通过开机短鸣、风险短鸣、参数错误短鸣和故障报警体现。
