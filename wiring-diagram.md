# 项目完整接线示意图

本文只整理当前方案的接线关系，权威引脚仍以 `pinmap.md` 和 `Auto-lift-wiring-design.md` 为准。

## 1. 总电源与共地

```text
220V AC
  │
  ▼
24V / 5A 开关电源
  │
  ├── 24V+ ───────────────┬── UM244 `V+`
  │                       ├── 四个 24V NPN 限位器棕线
  │                       ├── 四个限位光耦输入侧 `IN+` / 输入正端
  │                       └── 24V→5V 降压模块 `IN+`
  │
  └── 24V- / 0V ──────────┬── UM244 `V-`
                          ├── 四个 24V NPN 限位器蓝线
                          ├── 24V→5V 降压模块 `IN-`
                          ├── STM32 最小系统板 `GND`
                          ├── UM244 信号参考地 / 光耦输出侧 `GND`
                          └── OLED、WF5805F、蜂鸣器、按键公共 `GND`

24V→5V 降压模块
  │
  ├── 5V+ ────────────────┬── STM32 最小系统板 `5V` 输入
  │                       ├── UM244 `PU+`
  │                       ├── UM244 `DR+`
  │                       ├── UM244 `MF+`
  │                       └── 三个 UM244 控制光耦输出侧 `3V-24V` / 信号电源
  │
  └── 5V GND ─────────────┬── STM32 `GND`
                          ├── 24V 电源 0V
                          └── 三个 UM244 控制光耦输出侧 `GND`

STM32 最小系统板 3.3V
  │
  ├── OLED `VCC`
  ├── 三颗 WF5805F `VDD`
  ├── 蜂鸣器模块 `VCC`
  ├── 三个 UM244 控制光耦 MCU 输入侧 `VCC`
  ├── 四个限位光耦输出侧 `VCC`
  └── OLED / 三组 WF5805F I2C 的 SCL、SDA 上拉电阻
```

关键限制：

- 5V 只接 STM32 最小系统板 `5V` 输入、UM244 输入信号公共正端和 UM244 控制光耦输出侧，不接 STM32 GPIO。
- OLED、WF5805F、蜂鸣器模块都按 3.3V 供电。
- 限位器黑线不能直接接 STM32，只能进入 24V NPN 输入型光耦模块。

共地原则：

现阶段不是要求所有 GND 都必须拧在同一根线上，而是要求需要电气参考的模块之间必须有同一个系统 0V 参考。推荐在 24V 电源 0V 或控制盒端子排附近做低阻抗公共地/星形地，再分别引到各模块，避免让电机大电流回流路径穿过 STM32、传感器或 I2C 地线。

| 关系                                                 | 是否需要共地 | 说明                                                                                                      |
| ---------------------------------------------------- | ------------ | --------------------------------------------------------------------------------------------------------- |
| 24V 电源 `0V` 与 UM244 `V-`                          | 必须         | UM244 驱动器主电源回路需要完整 24V 回路                                                                   |
| 24V 电源 `0V` 与 24V->5V 降压模块 `IN-`              | 必须         | 5V 来自同一 24V 电源，降压模块输入负端接系统 0V                                                           |
| 24V->5V 降压模块 `5V GND` 与 STM32 `GND`             | 必须         | 5V 给最小系统板供电，STM32、3.3V 外设和信号光耦输入侧都以此为参考                                         |
| STM32 `GND` 与 OLED、三颗 WF5805F、蜂鸣器、按键、LED | 必须         | 这些外设直接接 STM32 GPIO/I2C/3.3V，必须共地；WF5805F 的 `GND` 必须接 STM32 `GND`                         |
| STM32 `GND` 与 UM244 控制光耦 MCU 输入侧 `GND`       | 必须         | STEP/DIR/MF 光耦输入侧由 STM32 GPIO 驱动，需要同一 3.3V 逻辑参考                                          |
| 5V `GND` 与 UM244 控制光耦输出侧 `GND`               | 必须         | 光耦输出侧用 5V 信号电源下拉 UM244 `PU-/DR-/MF-`，输出侧 GND 需要接 5V GND                                |
| ST-LINK `GND` 与 STM32 `GND`                         | 调试时必须   | 下载/调试时 SWD 信号必须有共同参考；系统已供电时 ST-LINK `3.3V` 只作目标电压参考                          |
| 24V 限位器蓝线与 24V 电源 `0V`                       | 必须         | 限位器本体需要 24V 供电回路；棕线接 +24V，蓝线接 24V 0V                                                   |
| 限位器黑线与 STM32 GPIO/GND                          | 禁止直接连接 | 黑线只能进 24V NPN 输入型光耦输入侧；STM32 只接光耦输出侧 `OUT-IO`                                        |
| 限位光耦输入侧 24V 地与输出侧 STM32 地               | 不直接跨接   | 通过光耦隔离传递信号；当前非隔离 24V->5V 方案下系统 0V 可能已共参考，但不要用限位黑线或输入侧端子绕过光耦 |
| UM244 电机输出 `A+/A-/B+/B-` 与 GND                  | 禁止连接     | 电机绕组端子不是地线，不能接 24V 0V、5V GND 或 STM32 GND                                                  |
| 220V AC `L/N` 与任意 DC GND                          | 禁止连接     | 市电火线/零线不能接低压 GND；若电源有 PE/接地端，按电源和安全规范接机壳/保护地                            |

## 2. STM32 引脚总表

| 外设/功能      | STM32 引脚       | 接线对象                  | 有效逻辑                          |
| -------------- | ---------------- | ------------------------- | --------------------------------- |
| OLED SCL       | `PB8`            | OLED `SCL`                | 软件 I2C                          |
| OLED SDA       | `PB9`            | OLED `SDA`                | 软件 I2C                          |
| `P_air` SCL    | `PA1`            | 空气参考 WF5805F `SCL`    | 软件 I2C                          |
| `P_air` SDA    | `PA2`            | 空气参考 WF5805F `SDA`    | 软件 I2C                          |
| `P_basket` SCL | `PB6`            | 框篮底部 WF5805F `SCL`    | 软件 I2C                          |
| `P_basket` SDA | `PB7`            | 框篮底部 WF5805F `SDA`    | 软件 I2C                          |
| `P_tank` SCL   | `PA8`            | 鱼缸底部 WF5805F `SCL`    | 软件 I2C                          |
| `P_tank` SDA   | `PA9`            | 鱼缸底部 WF5805F `SDA`    | 软件 I2C                          |
| UM244 STEP     | `PA3 / TIM2_CH4` | STEP 光耦输入侧 `IO`      | STM32 低电平使 UM244 `PU-` 被拉低 |
| UM244 DIR      | `PA4`            | DIR 光耦输入侧 `IO`       | 方向电平需实测确认                |
| UM244 MF/释放  | `PA5`            | MF 光耦输入侧 `IO`        | 默认高电平，不释放电机            |
| 左上限位       | `PB12`           | 左上限位光耦输出 `OUT-IO` | 低电平触发                        |
| 左下限位       | `PB13`           | 左下限位光耦输出 `OUT-IO` | 低电平触发                        |
| 右上限位       | `PB14`           | 右上限位光耦输出 `OUT-IO` | 低电平触发                        |
| 右下限位       | `PB15`           | 右下限位光耦输出 `OUT-IO` | 低电平触发                        |
| 蜂鸣器         | `PA0`            | 有源蜂鸣器 `I/O`          | 低电平鸣叫                        |
| 按键 1         | `PB1`            | 按键到 GND                | 低电平按下                        |
| 按键 2         | `PB11`           | 按键到 GND                | 低电平按下                        |
| 暂停/确认键    | `PB10`           | 按键到 GND                | 低电平按下                        |
| 页面/取消键    | `PB0`            | 按键到 GND                | 低电平按下                        |
| LED1           | `PA6`            | LED 负极/阴极             | 正极经限流电阻接 3.3V，低电平亮   |
| LED2           | `PA7`            | LED 负极/阴极             | 正极经限流电阻接 3.3V，低电平亮   |
| SWDIO          | `PA13`           | ST-LINK `SWDIO`           | 调试保留                          |
| SWCLK          | `PA14`           | ST-LINK `SWCLK`           | 调试保留                          |

LED1/LED2 的接法为：3.3V -> 限流电阻 -> LED 正极/阳极，LED 负极/阴极 -> STM32 GPIO。`PA6/PA7` 输出高电平时 LED 熄灭，输出低电平时 LED 点亮。

## 3. OLED 与三颗 WF5805F

```text
STM32 3.3V ───────┬── OLED `VCC`
                  ├── P_air `VDD`
                  ├── P_basket `VDD`
                  └── P_tank `VDD`

STM32 GND ────────┬── OLED `GND`
                  ├── P_air `GND`
                  ├── P_basket `GND`
                  └── P_tank `GND`

STM32 `PB8` ───────── OLED `SCL`
STM32 `PB9` ───────── OLED `SDA`

STM32 `PA1` ───────── P_air `SCL`
STM32 `PA2` ───────── P_air `SDA`

STM32 `PB6` ───────── P_basket `SCL`
STM32 `PB7` ───────── P_basket `SDA`

STM32 `PA8` ───────── P_tank `SCL`
STM32 `PA9` ───────── P_tank `SDA`
```

| 设备       | 位置                 | I2C 总线  | 地址说明                               |
| ---------- | -------------------- | --------- | -------------------------------------- |
| OLED       | 控制盒/面板          | `PB8/PB9` | 7-bit `0x3C`，常见 8-bit 写地址 `0x78` |
| `P_air`    | 控制盒内，暴露空气   | `PA1/PA2` | WF5805F 7-bit `0x6D`                   |
| `P_basket` | 框篮底部，随框篮移动 | `PB6/PB7` | WF5805F 7-bit `0x6D`                   |
| `P_tank`   | 鱼缸底部固定位置     | `PA8/PA9` | WF5805F 7-bit `0x6D`                   |

每条 I2C 总线的 `SCL/SDA` 各保留一组约 `4.7k` 上拉到 3.3V。WF5805F 封装 PCB 已包含上拉时，不要再重复并太多上拉。

## 4. UM244 控制信号光耦接线

需要三块单路光耦模块，分别用于 STEP、DIR、MF。

```text
STEP 信号：
STM32 `PA3` ── 光耦1 输入侧 `IO`
STM32 3.3V ── 光耦1 输入侧 `VCC`
5V+       ── 光耦1 输出侧 `3V-24V` / 信号电源
5V GND    ── 光耦1 输出侧 `GND`
光耦1 `OUT` ── UM244 `PU-`
5V+          ── UM244 `PU+`

DIR 信号：
STM32 `PA4` ── 光耦2 输入侧 `IO`
STM32 3.3V ── 光耦2 输入侧 `VCC`
5V+       ── 光耦2 输出侧 `3V-24V` / 信号电源
5V GND    ── 光耦2 输出侧 `GND`
光耦2 `OUT` ── UM244 `DR-`
5V+          ── UM244 `DR+`

MF / 电机释放信号：
STM32 `PA5` ── 光耦3 输入侧 `IO`
STM32 3.3V ── 光耦3 输入侧 `VCC`
5V+       ── 光耦3 输出侧 `3V-24V` / 信号电源
5V GND    ── 光耦3 输出侧 `GND`
光耦3 `OUT` ── UM244 `MF-`
5V+          ── UM244 `MF+`
```

若光耦模块输入侧有 `GND` 端，也接 STM32 `GND`；若模块输入侧只有 `VCC/IO`，按模块图接 `VCC/IO` 即可。

逻辑关系：

- 光耦会反相：STM32 输出低电平时，UM244 对应负端被拉低。
- STEP 空闲时 `PA3` 保持高电平，发脉冲时短暂拉低再恢复高电平。
- MF 默认保持高电平，不释放电机；只有维护模式明确允许时才释放。
- 调试时必须实测 `PU-/DR-/MF-` 有效低电平为 `0-0.5V`。

## 5. UM244 供电与双电机并联

```text
24V+ ───────────── UM244 `V+`
24V- / 0V ──────── UM244 `V-`

电机1 红线 A ─┐
电机2 红线 A ─┴── UM244 `A+`

电机1 黄线 C ─┐
电机2 黄线 C ─┴── UM244 `A-`

电机1 绿线 B ─┐
电机2 绿线 B ─┴── UM244 `B+`

电机1 蓝线 D ─┐
电机2 蓝线 D ─┴── UM244 `B-`
```

注意：

- `A-C` 是一相绕组，对应红-黄。
- `B-D` 是另一相绕组，对应绿-蓝。
- 两个电机同色线并到同一个 UM244 端子。
- 如果两个电机方向不一致，只交换其中一个电机的同一相两根线，例如只交换红/黄，或只交换绿/蓝。
- 调试初期 UM244 电流建议先从 `2.5A` 开始，细分按当前方案设置为 `1600 pulse/rev`。

## 6. 四个 24V NPN 限位器

每个限位器一套独立光耦输入通道。四个限位器不能并到同一个输入通道。

```text
24V+ ─────┬── 左上限位棕线
         ├── 左下限位棕线
         ├── 右上限位棕线
         └── 右下限位棕线

24V 0V ──┬── 左上限位蓝线
         ├── 左下限位蓝线
         ├── 右上限位蓝线
         └── 右下限位蓝线

左上限位黑线 ── 光耦L-UP 输入侧 `IN-` / 信号端
左下限位黑线 ── 光耦L-DN 输入侧 `IN-` / 信号端
右上限位黑线 ── 光耦R-UP 输入侧 `IN-` / 信号端
右下限位黑线 ── 光耦R-DN 输入侧 `IN-` / 信号端

24V+ ───────── 光耦L-UP/L-DN/R-UP/R-DN 输入侧 `IN+`

STM32 3.3V ─── 四个限位光耦输出侧 `VCC`
STM32 GND ──── 四个限位光耦输出侧 `GND`

光耦L-UP `OUT-IO` ── STM32 `PB12`
光耦L-DN `OUT-IO` ── STM32 `PB13`
光耦R-UP `OUT-IO` ── STM32 `PB14`
光耦R-DN `OUT-IO` ── STM32 `PB15`
```

| 限位器   | 棕线 | 蓝线   | 黑线               | 光耦输出到 STM32 |
| -------- | ---- | ------ | ------------------ | ---------------- |
| 左上限位 | +24V | 24V 0V | 对应光耦输入信号端 | `PB12`           |
| 左下限位 | +24V | 24V 0V | 对应光耦输入信号端 | `PB13`           |
| 右上限位 | +24V | 24V 0V | 对应光耦输入信号端 | `PB14`           |
| 右下限位 | +24V | 24V 0V | 对应光耦输入信号端 | `PB15`           |

逻辑关系：

- 未触发：STM32 GPIO 读高电平。
- 触发：限位器黑线输出低电平，光耦输出侧 `OUT-IO` 变低，STM32 GPIO 读低电平。
- 限位光耦输出侧 `VCC` 和任何上拉只能接 3.3V，不能接 5V 或 24V。

## 7. 蜂鸣器、按键、ST-LINK

蜂鸣器：

```text
STM32 3.3V ───── 有源蜂鸣器 `VCC`
STM32 GND ────── 有源蜂鸣器 `GND`
STM32 `PA0` ──── 有源蜂鸣器 `I/O`
```

`PA0` 高电平关闭，低电平鸣叫。若需要复位期间保持安静，可在 `PA0` 到 3.3V 加 `10k` 上拉。

按键：

```text
STM32 `PB1`  ── 按键1 ── GND
STM32 `PB11` ── 按键2 ── GND
STM32 `PB10` ── 暂停/确认键 ── GND
STM32 `PB0`  ── 页面/取消键 ── GND
```

按键按下为低电平，固件使用上拉输入和消抖。

ST-LINK 下载调试：

```text
ST-LINK `SWDIO` ── STM32 `PA13`
ST-LINK `SWCLK` ── STM32 `PA14`
ST-LINK `GND`   ── STM32 `GND`
ST-LINK `3.3V`  ── 仅作目标电压参考；若系统已由 5V 降压模块供电，不要再用 ST-LINK 给板子重复供电
```

## 8. 最终接线检查

- 24V 没有直接进入 STM32 GPIO、OLED、WF5805F、蜂鸣器或按键。
- 5V 没有直接进入 STM32 GPIO，也没有接到 OLED/WF5805F/蜂鸣器 `VCC`。
- STM32 最小系统板只从 `5V` 引脚接收 5V，不把 5V 接入 `3.3V` 引脚。
- OLED 独占 `PB8/PB9`，不与任何 WF5805F 共用 I2C。
- 三颗 WF5805F 分别位于 `PA1/PA2`、`PB6/PB7`、`PA8/PA9`，同地址传感器没有挂在同一总线上。
- UM244 `PU+/DR+/MF+` 均为 5V，`PU-/DR-/MF-` 由三块光耦分别下拉。
- 四个限位器每个都有独立光耦通道，输出侧只上拉到 3.3V。
- 两个电机按红/黄一相、绿/蓝一相并联到 UM244，禁止把红/绿或黄/蓝当作同一相。
- 上电前先空载确认 24V、5V、3.3V 极性和电压，再接入 STM32 与外设。
