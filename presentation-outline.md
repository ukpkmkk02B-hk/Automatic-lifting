# 鱼缸框篮自动升降控制项目提纲

## 1. 项目目标

本项目设计一个基于 STM32F103C8T6 的鱼缸框篮自动升降装置。

核心目标：

- 控制对象是框篮内实际可活动水深，不是整体鱼缸水位。
- 让多鳍鱼幼鱼从深水位逐步适应浅水位。
- 默认从 `100mm` 逐步过渡到 `10mm`。
- 默认每日变浅 `1mm/day`，控制误差目标为 `±1mm`。

## 2. 系统总体方案

系统由传感、控制、执行、安全和人机交互五部分组成。

- 传感：三颗 WF5805F 压力传感器。
- 控制：STM32F103C8T6 最小系统板。
- 执行：UM244 驱动器同步驱动两个贯穿式步进电机。
- 安全：四个 24V NPN 限位器、蜂鸣器报警、故障状态机。
- 交互：OLED 显示和按键设置/维护。

## 3. 闭环控制与自动仲裁结构

```mermaid
flowchart LR
    A["目标水深曲线<br/>100mm -> 10mm<br/>默认 1mm/day"] --> B["STM32 状态机<br/>安全联锁 / DROP / TRK / NAP 仲裁"]

    P1["P_air<br/>空气参考"] --> C["压力差换算"]
    P2["P_basket<br/>框篮底部"] --> C
    P3["P_tank<br/>鱼缸底部"] --> C

    C --> D["basket_depth_mm<br/>框篮实际水深"]
    C --> E["tank_depth_mm<br/>鱼缸整体水深"]

    D --> B
    E --> B

    B --> F["UM244 STEP/DIR/MF"]
    F --> G["两个步进电机同步运动"]
    G --> H["框篮高度变化"]
    H --> I["框篮内水深变化"]
    I --> P2

    L["四个限位器"] --> S["安全联锁"]
    C --> S
    S --> B
    S --> Z["停止运动 / OLED显示 / 蜂鸣器报警"]
```

讲解重点：

- 传感器测量实际水深。
- 控制器比较实际水深和目标水深。
- 自动控制内部由仲裁器决定本轮唯一动作，不允许多个控制策略同时抢占电机。
- 步进电机改变框篮高度。
- 框篮高度变化反过来改变水深，形成闭环。
- 限位器和异常检测独立进入安全联锁。

## 4. 水深测量方案

WF5805F 是绝对压力传感器，读数包含大气压，所以增加空气参考传感器。

三颗传感器分工：

| 传感器     | 位置           | 作用                     |
| ---------- | -------------- | ------------------------ |
| `P_air`    | 控制盒内空气中 | 去除大气压影响           |
| `P_tank`   | 鱼缸底部       | 判断整体鱼缸水深是否安全 |
| `P_basket` | 框篮底部       | 计算鱼实际可活动水深     |

计算逻辑：

```text
basket_depth = ((P_basket - P_air) - zero_offset_basket) * 10.197
tank_depth   = ((P_tank - P_air) - zero_offset_tank) * 10.197
```

`zero_offset_basket/tank` 来自维护模式 `CAL AIR`：三颗传感器同处空气时，记录 `BAS/TNK` 相对 `AIR` 的固定偏移。

## 5. 执行机构设计

执行机构采用一个 UM244 驱动器同步控制两个贯穿式丝杆步进电机。

关键参数：

- 丝杆导程：`2mm/rev`
- 驱动细分：`1600 pulse/rev`
- 位移换算：`800 pulse/mm`
- 单次打盹：默认 `8 pulse = 0.01mm`
- 机械最大行程：`170mm`
- 自动目标水深范围：`5-100mm`

电机不连续极慢速运行，而是采用“打盹模式”。

## 6. 自动控制内部优先级与控制策略

自动控制不是让多个功能同时驱动电机，而是由 `auto_control` 仲裁器在每轮主循环中输出一个结果：

```text
安全联锁
限位 / 传感器 / I2C / 水位范围 / 位置可信 / 电机释放
        │
        ▼
自动控制仲裁器(优先级)
        │
        ├─ 1. 危险掉水或不可跟随突变 → FAULT
        │
        ├─ 2. 快速掉水跟随 DROP → 框篮下降补深 → PAUS + CHECK WATER
        │
        ├─ 3. 低频闭环修正 TRK → 小步上升/下降修正实际水深
        │
        ├─ 4. 每日打盹 NAP → 按计划让水深每天变浅
        │
        └─ 5. 无动作 → AUTO 等待显示
```

优先级说明：

| 优先级 | 状态     | 触发条件                                            | 电机动作       | 退出结果                                        |
| -----: | -------- | --------------------------------------------------- | -------------- | ----------------------------------------------- |
|      0 | 安全联锁 | 限位、传感器、I2C、水位范围、位置可信、电机释放异常 | 停止           | `FAULT` 或禁止自动                              |
|      1 | `DROP`   | 鱼缸水位 `5-30mm/min` 下降，且框篮水深同步变浅      | 只允许下降补深 | 稳定、超时或超距离后 `PAUS`，显示 `CHECK WATER` |
|      2 | `TRK`    | 普通水深偏差达到 `±2.0mm`                           | 小步上升或下降 | 修正到 `±1.0mm` 内回 `AUTO`                     |
|      3 | `NAP`    | 到达每日计划打盹时间，且当天仍有变浅额度            | 只上升变浅     | 完成有限脉冲后回 `AUTO` 等待                    |
|      4 | `AUTO`   | 没有更高优先级事件                                  | 不运动         | 等待下一轮判断                                  |

这样设计的原因：

- 幼鱼体型小，水深变化必须非常缓慢。
- 快速掉水会让框篮内水深短时间变浅，因此优先级高于普通纠偏和每日计划。
- 低频闭环只处理普通小偏差，避免传感器噪声导致频繁追调。
- 每日打盹是计划性适应动作，只有系统稳定时才允许执行。

关键参数：

- `NAP`：默认每隔约 `14.4min` 执行 `8 pulse`，约 `0.01mm`，脉冲频率 `800Hz`。
- `TRK`：每约 `3min` 检查一次；`1-2mm` 只观察并阻止继续变浅；超过 `2mm` 小步修正；超过 `5mm` 报警。
- `DROP`：使用 `30s` 水位趋势；`5-30mm/min` 可跟随，超过 `30mm/min` 作为危险掉水报警。
- 今日进度只统计自动变浅动作：`NAP` 向上和 `TRK` 向上计入，并共享当日剩余变浅额度；`TRK` 向下、`DROP`、手动、回零都不计入。

菜单可调参数范围：

| 参数           |    默认值 | 可调范围        | 汇报说明                           |
| -------------- | --------: | --------------- | ---------------------------------- |
| 初始目标水深   |     100mm | `5.0-100.0mm`   | 不能小于最终目标水深               |
| 最终目标水深   |      10mm | `5.0-100.0mm`   | 不能大于初始目标水深               |
| 每日变浅量     | 1.0mm/day | `0.0-3.0mm/day` | 防止误设过快，仍受最小打盹间隔限制 |
| 单次打盹脉冲数 |   8 pulse | `1-24 pulse`    | 影响每次微动位移和理论打盹间隔     |

`TRK` 和 `DROP` 的阈值当前通过 `board_config.h` 宏配置，属于重新编译可调，不是 OLED 菜单现场可调。

## 7. 安全设计

安全设计优先级高于自动控制内部优先级。

必须停止运动或停止自动运行的情况：

- 上升方向触发任意上限位。
- 下降方向触发任意下限位。
- 左右同方向限位状态不一致。
- 任一压力传感器连续读取失败。
- I2C 总线恢复失败。
- 鱼缸水位过低/过高。
- 框篮水深低于安全范围。
- 水位突变或卡滞趋势异常。
- 快速掉水跟随结束后未人工确认。

报警方式：

- OLED 显示错误原因。
- 蜂鸣器报警。
- 蜂鸣器可静音，但故障状态不清除。

## 8. 固件架构

固件采用三层分离和非阻塞设计。

```mermaid
flowchart TB
    A["安全底层<br/>限位急停 / STEP中断保护"] --> B["驱动服务层<br/>I2C / 传感器 / OLED / 按键 / 步进 / Flash"]
    B --> C["应用状态机<br/>自检 / 自动仲裁 / 维护 / 故障"]
```

关键原则：

- 不使用 `Delay` 做业务等待。
- 打盹间隔用时间戳调度。
- STEP 脉冲用定时器输出。
- 限位保护在 STEP 中断中直接检查。
- I2C 必须有超时和总线恢复。
- `auto_control` 只输出 `MOVE`、`PAUSE`、`FAULT` 或 `NONE` 决策，真正的 STEP 输出集中由步进驱动模块执行。

## 9. 断电恢复策略

当前版本不增加 RTC 和 UPS。

恢复原则：

- 断电期间不补偿错过的位移。
- 重启后读取当前水深和 Flash 参数。
- 若自检通过、传感器正常、限位正常、位置可信，则继续按设定速度运行。
- 若水深差异异常或参数无效，则进入暂停报警，等待人工确认。

## 10. 项目亮点与风险

项目亮点：

- 控制框篮水深，而不是控制整个鱼缸水位。
- 使用三压力传感器消除大气压影响。
- `DROP / TRK / NAP` 固定优先级仲裁，兼顾掉水保护、普通纠偏和每日适应曲线。
- 打盹式微位移适合幼鱼缓慢适应。
- 限位、传感器、I2C、状态机多层安全保护。
- 断电后可恢复控制，不追赶断电期间进度。

主要风险：

- WF5805F 防水封装会直接影响测量准确性。
- 水下 I2C 线缆容易受电机噪声影响。
- 两个电机并联一个驱动器，同步性需要实测验证。
- 无独立电流检测，卡滞只能通过水深趋势间接判断。
- `DROP` 和 `TRK` 参数需要实物水位波动测试后继续微调。

## 11. 建议汇报顺序

1. 先说明为什么要控制框篮水深。
2. 再说明传感器如何测出真实水深。
3. 然后讲步进电机如何改变框篮高度。
4. 用闭环图说明“测量、判断、执行、反馈”。
5. 重点讲自动控制内部优先级：安全联锁、`DROP`、`TRK`、`NAP`。
6. 说明今日进度如何统计，解释为什么补深和掉水跟随不计入每日变浅量。
7. 最后强调安全保护、断电恢复和当前风险。


## 12. 核心的控制闭环：
                ┌────────────────────────────┐
                │        鱼缸 / 框篮系统       │
                │  框篮高度变化 → 框篮内水深变化 │
                └────────────┬───────────────┘
                             │
                             │ 水压变化
                             ▼
        ┌────────────────────────────────────────────┐
        │               三颗压力传感器               │
        │ P_air：空气参考                             │
        │ P_tank：鱼缸底部水深                        │
        │ P_basket：框篮底部实际水深                  │
        └────────────┬────────────┬─────────────────┘
                     │            │
                     │ I2C        │ I2C
                     ▼            ▼
               ┌────────────────────────┐
               │   STM32F103C8T6 主控   │
               │ 计算：                  │
               │ basket_depth = ((P_basket-P_air)-zero_B)*10.197 │
               │ tank_depth = ((P_tank-P_air)-zero_T)*10.197     │
               │                                  │
               │ 安全联锁检查通过后执行自动仲裁：  │
               │ DROP > TRK > NAP > AUTO等待       │
               │ 决定是否输出 STEP / DIR / MF      │
               └────────────┬────────────────────┘
                            │
                            │ STEP / DIR / MF
                            ▼
                ┌────────────────────────────┐
                │  单路光耦隔离模块 ×3       │
                │  STEP→PU-                  │
                │  DIR →DR-                  │
                │  MF  →MF-                  │
                └────────────┬───────────────┘
                             │
                             │ 共阳控制信号
                             ▼
                   ┌────────────────────┐
                   │     UM244 驱动器    │
                   │ 24V供电，5V信号输入 │
                   └──────────┬─────────┘
                              │
                              │ A+/A-/B+/B-
                              ▼
              ┌──────────────────────────────────┐
              │ 两个并联贯穿式步进电机 + 丝杆机构 │
              │ 电机转动 → 丝杆移动 → 框篮升降     │
              └──────────────────┬───────────────┘
                                 │
                                 │ 框篮位置改变
                                 ▼
                      框篮内实际可活动水深改变
                                 │
                                 └────→ 再次被 P_basket 检测
